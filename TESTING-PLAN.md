# 🧪 ПЛАН ТЕСТИРОВАНИЯ СИСТЕМЫ УПРАВЛЕНИЯ НОВОСТЯМИ

## 📋 БЫСТРЫЙ СТАРТ

### Windows:

```cmd
cd kfa-backend\kfa-api
setup-news-system.bat
```

### Linux/Mac:

```bash
cd kfa-backend/kfa-api
chmod +x setup-news-system.sh
./setup-news-system.sh
```

---

## ✅ CHECKLIST ПЕРЕД ТЕСТИРОВАНИЕМ

- [ ] Backend запущен: `php artisan serve` (http://localhost:8000)
- [ ] Frontend запущен: `npm run dev` (http://localhost:5173)
- [ ] База данных настроена в `.env`
- [ ] Миграции выполнены: `php artisan migrate`
- [ ] Права назначены роли admin
- [ ] Пользователь admin создан и может войти в систему

---

## 🔍 ТЕСТИРОВАНИЕ BACKEND API

### 1. Публичные Endpoints (без авторизации)

#### 1.1 Получить список новостей

```bash
curl -X GET http://localhost:8000/api/news \
  -H "Accept: application/json"
```

**Ожидаемый результат:**

```json
{
  "data": [],
  "current_page": 1,
  "last_page": 1,
  "per_page": 15,
  "total": 0
}
```

#### 1.2 Получить одну новость

```bash
curl -X GET http://localhost:8000/api/news/1 \
  -H "Accept: application/json"
```

**Ожидаемый результат:** 404 (если новостей нет) или JSON с новостью

---

### 2. Авторизация

#### 2.1 Логин и получение токена

```bash
curl -X POST http://localhost:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@example.com",
    "password": "password"
  }'
```

**Сохраните токен из ответа!** Он понадобится для следующих тестов.

**Экспорт токена (Linux/Mac):**

```bash
export TOKEN="your_token_here"
```

**Экспорт токена (Windows CMD):**

```cmd
set TOKEN=your_token_here
```

---

### 3. Защищенные Endpoints (требуют авторизации)

#### 3.1 Создать новость

```bash
curl -X POST http://localhost:8000/api/news \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовая новость",
    "content": "Это содержимое тестовой новости для проверки системы.",
    "excerpt": "Краткое описание новости",
    "status": "draft",
    "featured": false
  }'
```

**Ожидаемый результат:**

```json
{
  "data": {
    "id": 1,
    "title": "Тестовая новость",
    "slug": "testovaya-novost",
    "content": "Это содержимое тестовой новости...",
    "status": "draft",
    "featured": false,
    "reading_time": 1,
    "author": {
      "id": 1,
      "name": "Admin",
      "email": "admin@example.com"
    },
    "is_published": false,
    "is_draft": true,
    "can_edit": true
  }
}
```

#### 3.2 Получить статистику

```bash
curl -X GET http://localhost:8000/api/news/stats \
  -H "Authorization: Bearer $TOKEN" \
  -H "Accept: application/json"
```

**Ожидаемый результат:**

```json
{
  "total": 1,
  "published": 0,
  "draft": 1,
  "pending": 0,
  "featured": 0,
  "archived": 0
}
```

#### 3.3 Обновить новость

```bash
curl -X PUT http://localhost:8000/api/news/1 \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Обновленная новость",
    "content": "Обновленное содержимое",
    "status": "published"
  }'
```

#### 3.4 Опубликовать новость

```bash
curl -X POST http://localhost:8000/api/news/1/publish \
  -H "Authorization: Bearer $TOKEN"
```

#### 3.5 Одобрить новость

```bash
curl -X POST http://localhost:8000/api/news/1/approve \
  -H "Authorization: Bearer $TOKEN"
```

#### 3.6 Переключить избранное

```bash
curl -X POST http://localhost:8000/api/news/1/toggle-featured \
  -H "Authorization: Bearer $TOKEN"
```

#### 3.7 Удалить новость

```bash
curl -X DELETE http://localhost:8000/api/news/1 \
  -H "Authorization: Bearer $TOKEN"
```

---

### 4. Управление медиафайлами

#### 4.1 Загрузить изображение

```bash
curl -X POST http://localhost:8000/api/media \
  -H "Authorization: Bearer $TOKEN" \
  -F "file=@/path/to/image.jpg" \
  -F "collection=news"
```

**Сохраните media_id из ответа!**

#### 4.2 Прикрепить главное изображение к новости

```bash
curl -X POST http://localhost:8000/api/news/1/media \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "media_id": 1,
    "type": "featured",
    "order": 0
  }'
```

#### 4.3 Прикрепить изображение в галерею

```bash
curl -X POST http://localhost:8000/api/news/1/media \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "media_id": 2,
    "type": "gallery",
    "order": 1
  }'
```

#### 4.4 Изменить порядок изображений в галерее

```bash
curl -X PUT http://localhost:8000/api/news/1/media/reorder \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "media_ids": [3, 2, 1]
  }'
```

#### 4.5 Открепить изображение

```bash
curl -X DELETE http://localhost:8000/api/news/1/media/2 \
  -H "Authorization: Bearer $TOKEN"
```

---

### 5. Фильтрация и поиск

#### 5.1 Поиск по заголовку

```bash
curl -X GET "http://localhost:8000/api/news?search=тестовая" \
  -H "Accept: application/json"
```

#### 5.2 Фильтр по статусу

```bash
curl -X GET "http://localhost:8000/api/news?status=published" \
  -H "Accept: application/json"
```

#### 5.3 Фильтр по автору

```bash
curl -X GET "http://localhost:8000/api/news?author_id=1" \
  -H "Accept: application/json"
```

#### 5.4 Только избранные

```bash
curl -X GET "http://localhost:8000/api/news?featured=1" \
  -H "Accept: application/json"
```

#### 5.5 Сортировка

```bash
curl -X GET "http://localhost:8000/api/news?sort_by=published_at&sort_order=desc" \
  -H "Accept: application/json"
```

#### 5.6 Пагинация

```bash
curl -X GET "http://localhost:8000/api/news?page=2&per_page=10" \
  -H "Accept: application/json"
```

---

## 🎨 ТЕСТИРОВАНИЕ FRONTEND

### 1. Проверка навигации

1. Откройте браузер: `http://localhost:5173`
2. Войдите как администратор
3. Проверьте, что в боковом меню есть раздел "Управление контентом" с пунктами:
   - ✅ Новости
   - ✅ События
   - ✅ Участники
   - ✅ Медиафайлы
   - ✅ Партнеры
   - ✅ Настройки

### 2. Страница News Manager

1. Кликните на "Новости" в меню
2. Откроется страница: `http://localhost:5173/dashboard/news`

**Проверьте:**

- [ ] Страница загружается без ошибок
- [ ] Видна кнопка "Добавить новость"
- [ ] Есть поля поиска и фильтрации
- [ ] Таблица с новостями (может быть пустой)

### 3. Создание новости

1. Нажмите "Добавить новость"
2. Заполните форму:
   - Заголовок: "Тестовая новость"
   - Содержимое: "Это тестовое содержимое"
   - Статус: Draft
3. Нажмите "Сохранить"

**Проверьте:**

- [ ] Форма валидируется корректно
- [ ] Новость появилась в списке
- [ ] Slug сгенерировался автоматически
- [ ] Статус отображается правильно

### 4. Добавление изображения

1. Откройте созданную новость на редактирование
2. Нажмите на кнопку выбора изображения
3. Откроется MediaPicker
4. Загрузите изображение
5. Выберите его
6. Сохраните новость

**Проверьте:**

- [ ] Изображение загрузилось
- [ ] Изображение прикрепилось к новости
- [ ] Превью изображения отображается

### 5. Редактирование новости

1. Нажмите на кнопку "Редактировать" у созданной новости
2. Измените заголовок
3. Сохраните

**Проверьте:**

- [ ] Изменения сохранились
- [ ] Данные обновились в списке

### 6. Изменение статуса

1. Откройте новость
2. Измените статус на "Published"
3. Сохраните

**Проверьте:**

- [ ] Статус изменился
- [ ] Цвет статуса обновился
- [ ] В статистике обновились цифры

### 7. Удаление новости

1. Нажмите кнопку "Удалить" у новости
2. Подтвердите удаление

**Проверьте:**

- [ ] Появилось подтверждение
- [ ] Новость удалилась из списка
- [ ] Статистика обновилась

---

## 🔐 ТЕСТИРОВАНИЕ ПРАВ ДОСТУПА

### 1. Как администратор (роль: admin)

**Должны быть доступны:**

- ✅ Просмотр всех новостей
- ✅ Создание новостей
- ✅ Редактирование любых новостей
- ✅ Удаление любых новостей
- ✅ Модерация (approve, reject, publish, archive)
- ✅ Управление медиафайлами
- ✅ Просмотр статистики

### 2. Как обычный пользователь (роль: member)

**Должны быть доступны:**

- ✅ Просмотр опубликованных новостей (публичный доступ)
- ❌ Создание новостей (403 Forbidden)
- ❌ Редактирование новостей (403 Forbidden)
- ❌ Удаление новостей (403 Forbidden)

### 3. Как гость (без авторизации)

**Должны быть доступны:**

- ✅ Просмотр опубликованных новостей
- ❌ Любые другие операции (401 Unauthorized)

---

## 📊 ПРОВЕРКА БАЗЫ ДАННЫХ

### 1. Таблица news

```sql
SELECT id, title, slug, status, featured, author_id, featured_image_id
FROM news;
```

**Проверьте:**

- [ ] Новости сохраняются
- [ ] Slug генерируется автоматически
- [ ] author_id заполняется
- [ ] featured_image_id может быть NULL

### 2. Таблица mediable

```sql
SELECT * FROM mediable WHERE mediable_type = 'App\\Models\\News';
```

**Проверьте:**

- [ ] Записи создаются при прикреплении медиа
- [ ] Поле `type` имеет значение 'featured' или 'gallery'
- [ ] Поле `order` заполняется правильно
- [ ] Timestamps обновляются

### 3. Таблица media

```sql
SELECT id, file_name, mime_type, size, collection_name
FROM media
WHERE id IN (SELECT media_id FROM mediable WHERE mediable_type = 'App\\Models\\News');
```

**Проверьте:**

- [ ] Медиафайлы сохраняются
- [ ] collection_name соответствует типу ('news', 'news-gallery')

---

## 🐛 ПРОВЕРКА ОБРАБОТКИ ОШИБОК

### 1. Валидация формы (Frontend)

**Попробуйте:**

- Отправить пустую форму
- Заголовок меньше 3 символов
- Заголовок больше 255 символов
- Контент меньше 10 символов
- Excerpt больше 500 символов

**Проверьте:**

- [ ] Появляются сообщения об ошибках
- [ ] Форма не отправляется
- [ ] Ошибки на русском языке

### 2. Валидация API (Backend)

```bash
# Создание новости без заголовка
curl -X POST http://localhost:8000/api/news \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"content": "Только контент"}'
```

**Ожидаемый результат:** 422 Unprocessable Entity с описанием ошибки

### 3. Авторизация

```bash
# Попытка создать новость без токена
curl -X POST http://localhost:8000/api/news \
  -H "Content-Type: application/json" \
  -d '{"title": "Test", "content": "Content"}'
```

**Ожидаемый результат:** 401 Unauthorized

### 4. Несуществующий ресурс

```bash
curl -X GET http://localhost:8000/api/news/99999 \
  -H "Accept: application/json"
```

**Ожидаемый результат:** 404 Not Found

---

## ✅ КРИТЕРИИ УСПЕШНОГО ТЕСТИРОВАНИЯ

### Backend API ✅

- [ ] Все публичные endpoints работают без ошибок
- [ ] Авторизация работает корректно
- [ ] CRUD операции выполняются успешно
- [ ] Модерация работает для роли admin
- [ ] Управление медиа работает корректно
- [ ] Фильтрация и поиск возвращают правильные результаты
- [ ] Статистика отображается корректно
- [ ] Валидация работает на всех endpoints
- [ ] Права доступа соблюдаются

### Frontend ✅

- [ ] Страница загружается без ошибок
- [ ] Навигация работает
- [ ] Форма создания/редактирования работает
- [ ] MediaPicker интегрирован
- [ ] Таблица новостей отображается
- [ ] Фильтры работают
- [ ] Пагинация работает
- [ ] Статусы отображаются правильно
- [ ] Валидация работает на клиенте

### Интеграция ✅

- [ ] Frontend корректно взаимодействует с Backend API
- [ ] Токены авторизации передаются правильно
- [ ] Ошибки API обрабатываются на Frontend
- [ ] Обновление данных в реальном времени
- [ ] Права доступа соблюдаются на обеих сторонах

---

## 📝 ОТЧЕТ О ТЕСТИРОВАНИИ

После завершения тестирования заполните:

**Дата тестирования:** ******\_\_\_******

**Версия Backend:** ******\_\_\_******

**Версия Frontend:** ******\_\_\_******

**Результаты:**

| Категория        | Пройдено | Провалено | Заметки |
| ---------------- | -------- | --------- | ------- |
| Backend API      | ** / **  | ** / **   |         |
| Frontend         | ** / **  | ** / **   |         |
| Интеграция       | ** / **  | ** / **   |         |
| Права доступа    | ** / **  | ** / **   |         |
| Обработка ошибок | ** / **  | ** / **   |         |

**Найденные проблемы:**

1.
2.
3.

**Рекомендации:**

1.
2.
3.

---

## 🚀 ГОТОВО К ПРОДАКШЕНУ?

Система готова к развертыванию на продакшене, если:

- ✅ Все тесты пройдены успешно
- ✅ Нет критических багов
- ✅ Права доступа настроены правильно
- ✅ Валидация работает корректно
- ✅ Обработка ошибок реализована
- ✅ Документация обновлена
- ✅ Backup базы данных настроен

**Удачи! 🎉**
