# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ СИСТЕМЫ АВТОРИЗАЦИИ

## 📋 ОГЛАВЛЕНИЕ

1. [Подготовка к тестированию](#подготовка)
2. [Вариант 1: Автоматическое тестирование (Node.js)](#автоматическое-тестирование)
3. [Вариант 2: Интерактивное тестирование (Браузер)](#интерактивное-тестирование)
4. [Вариант 3: Ручное тестирование через frontend](#ручное-тестирование)
5. [Что должно работать](#что-должно-работать)
6. [Возможные проблемы](#возможные-проблемы)

---

## 📦 ПОДГОТОВКА

### Шаг 1: Запустите Backend

```bash
# Откройте первый терминал
cd kfa-backend/kfa-api

# Запустите Laravel сервер
php artisan serve

# Сервер должен запуститься на http://localhost:8000
```

**Проверка:** Откройте http://localhost:8000/up в браузере. Должно появиться `{"status":"ok"}`

### Шаг 2: Проверьте базу данных

```bash
# В том же терминале
php artisan migrate:status

# Если миграции не выполнены:
php artisan migrate

# Опционально: Создать тестовых пользователей
php artisan db:seed --class=RolesAndPermissionsSeeder
php artisan db:seed --class=DevUsersSeeder
```

### Шаг 3 (Опционально): Запустите Frontend

```bash
# Откройте второй терминал
cd kfa-website

# Установите зависимости (если еще не установлены)
npm install

# Запустите dev сервер
npm run dev

# Сервер должен запуститься на http://localhost:3000
```

---

## 🤖 АВТОМАТИЧЕСКОЕ ТЕСТИРОВАНИЕ

### Что это?

Скрипт `test-auth-system.js` автоматически протестирует все сценарии через API:
- ✅ Отклонение слабых паролей
- ✅ Принятие сильных паролей
- ✅ Регистрация нового пользователя
- ✅ Вход в систему
- ✅ Работа с токенами
- ✅ Выход из системы

### Запуск

```bash
# Убедитесь, что backend запущен!
# Затем в корне проекта:
node test-auth-system.js
```

### Ожидаемый результат

```
╔════════════════════════════════════════════════════════════╗
║     АВТОМАТИЗИРОВАННОЕ ТЕСТИРОВАНИЕ АВТОРИЗАЦИИ KFA       ║
╚════════════════════════════════════════════════════════════╝

============================================================
ТЕСТ: Проверка доступности API
============================================================
✅ УСПЕХ: Backend доступен

============================================================
ТЕСТ: Регистрация со слабым паролем (только буквы)
============================================================
✅ УСПЕХ: Слабый пароль отклонен
ℹ️  INFO: Ошибка: Пароль должен содержать хотя бы одну букву и одну цифру.

... (10 тестов) ...

╔════════════════════════════════════════════════════════════╗
║                    ИТОГОВЫЙ ОТЧЕТ                         ║
╚════════════════════════════════════════════════════════════╝

Всего тестов: 10
✅ Пройдено: 10
❌ Провалено: 0

🎉 УСПЕХ: 100.0% тестов пройдено!
Система авторизации работает корректно!
```

---

## 🌐 ИНТЕРАКТИВНОЕ ТЕСТИРОВАНИЕ

### Что это?

HTML страница `test-auth-ui.html` с красивым UI для тестирования через браузер.

### Запуск

1. **Убедитесь, что backend запущен** на http://localhost:8000
2. **Откройте файл в браузере:**
   ```
   test-auth-ui.html
   ```
   Или двойной клик на файле

### Как использовать

#### Тест 1: Слабый пароль (только буквы)
- Email уже заполнен: `test1@kfa-test.kg`
- Пароль: `password`
- Нажмите **"Протестировать"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Слабый пароль отклонен!"

#### Тест 2: Слабый пароль (только цифры)
- Email: `test2@kfa-test.kg`
- Пароль: `12345678`
- Нажмите **"Протестировать"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Слабый пароль отклонен!"

#### Тест 3: Сильный пароль (регистрация)
- Имя: `Тестовый Пользователь`
- Email: генерируется автоматически
- Пароль: `MyPass2024`
- Нажмите **"Зарегистрировать"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Регистрация успешна!"
- **Результат:** Токен сохранен, email автоматически подставлен в форму входа

#### Тест 4: Вход в систему
- Email: автоматически из регистрации
- Пароль: `MyPass2024`
- Нажмите **"Войти"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Вход выполнен успешно!"

#### Тест 5: Получение данных с токеном
- Токен сохранен из предыдущих тестов
- Нажмите **"Получить данные"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Данные получены!"
- Отображаются: имя, email, роли пользователя

#### Тест 6: Выход
- Нажмите **"Выйти"**
- **Ожидается:** ✅ "ТЕСТ ПРОЙДЕН: Выход выполнен!"
- Затем повторите Тест 5 - должна быть ошибка (токен недействителен)

---

## 🖱️ РУЧНОЕ ТЕСТИРОВАНИЕ

### Через основной интерфейс приложения

#### 1. Тест регистрации

**Откройте:** http://localhost:3000/auth/register

**Тест A - Слабый пароль (только буквы):**
- Имя: Тест
- Фамилия: Пользователь
- Email: test@example.com
- Телефон: +996 555 123456
- Компания: Test Company
- Пароль: `password`
- Подтверждение: `password`
- ✅ Согласие с условиями
- Нажмите "Зарегистрироваться"

**Ожидается:**
```
❌ Пароль должен содержать хотя бы одну букву и одну цифру
```

**Тест B - Слабый пароль (только цифры):**
- Пароль: `12345678`
- Подтверждение: `12345678`

**Ожидается:**
```
❌ Пароль должен содержать хотя бы одну букву и одну цифру
```

**Тест C - Короткий пароль:**
- Пароль: `pass12`
- Подтверждение: `pass12`

**Ожидается:**
```
❌ Минимум 8 символов
```

**Тест D - Сильный пароль (УСПЕХ):**
- Email: `newuser@example.com`
- Пароль: `MyPass2024`
- Подтверждение: `MyPass2024`
- Нажмите "Зарегистрироваться"

**Ожидается:**
```
✅ Перенаправление на /dashboard
✅ Пользователь вошел в систему
```

#### 2. Тест входа

**Откройте:** http://localhost:3000/auth/login

**Тест A - Неверные данные:**
- Email: `wrong@email.com`
- Пароль: `wrongpass123`
- Нажмите "Войти"

**Ожидается:**
```
❌ The provided credentials are incorrect.
```

**Тест B - Правильные данные:**
- Email: `newuser@example.com` (из регистрации)
- Пароль: `MyPass2024`
- Нажмите "Войти"

**Ожидается:**
```
✅ Перенаправление на /dashboard
✅ В правом верхнем углу отображается имя пользователя
```

**Тест C - Быстрый вход (DEV MODE):**
- Если видите кнопки быстрого входа (DEV MODE):
  - Нажмите любую кнопку (Admin, Editor, etc.)
  - Пароль: `password`

**Ожидается:**
```
✅ Быстрый вход выполнен
```

#### 3. Тест токенов

**После успешного входа:**

1. **Откройте DevTools** (F12)
2. **Перейдите на вкладку Application/Storage**
3. **Найдите localStorage:**
   - Должен быть `auth_token`
   - Должен быть `kfa-auth-storage`

4. **Проверьте токен:**
   ```javascript
   // В Console DevTools
   localStorage.getItem('auth_token')
   // Должен вернуть строку токена
   ```

5. **Перезагрузите страницу** (F5)
   - **Ожидается:** Пользователь остается залогиненным

6. **Нажмите "Выйти"** (кнопка в шапке)
   - **Ожидается:**
     ```
     ✅ Перенаправление на /
     ✅ localStorage очищен
     ✅ Токен удален из базы данных
     ```

7. **Попробуйте зайти на /dashboard**
   - **Ожидается:**
     ```
     ✅ Перенаправление на /auth/login
     ```

---

## ✅ ЧТО ДОЛЖНО РАБОТАТЬ

### Валидация паролей

| Пароль | Результат | Причина |
|--------|-----------|---------|
| `password` | ❌ Отклонен | Только буквы |
| `12345678` | ❌ Отклонен | Только цифры |
| `pass12` | ❌ Отклонен | Менее 8 символов |
| `Pass12` | ✅ Принят | 6 букв + 2 цифры = 8 символов |
| `password123` | ✅ Принят | Буквы + цифры, 11 символов |
| `MyPass2024` | ✅ Принят | Буквы + цифры, 10 символов |
| `admin1234` | ✅ Принят | Буквы + цифры, 9 символов |

### Регистрация

- ✅ Создается новый пользователь в БД
- ✅ Пароль хешируется (bcrypt)
- ✅ Присваивается роль `guest` по умолчанию
- ✅ Возвращается токен + данные пользователя
- ✅ Frontend сохраняет токен в localStorage
- ✅ Автоматическое перенаправление на /dashboard

### Вход

- ✅ Проверка email + password
- ✅ Генерация нового токена
- ✅ Возврат данных пользователя с ролями/правами
- ✅ Frontend сохраняет токен
- ✅ Перенаправление на dashboard

### Токены

- ✅ Срок действия: 24 часа (1440 минут)
- ✅ Хранятся в таблице `personal_access_tokens`
- ✅ Передаются через заголовок `Authorization: Bearer {token}`
- ✅ Валидируются middleware `auth:sanctum`

### Защита маршрутов

- ✅ `/api/user` требует токен
- ✅ `/api/logout` требует токен
- ✅ `/api/login` и `/api/register` доступны без токена
- ✅ Frontend ProtectedRoute проверяет авторизацию

### Выход

- ✅ Удаляет текущий токен из БД
- ✅ Frontend очищает localStorage
- ✅ Перенаправление на главную

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема: Backend недоступен

**Симптомы:**
```
❌ ОШИБКА ПОДКЛЮЧЕНИЯ
Failed to fetch
```

**Решение:**
1. Убедитесь, что backend запущен: `php artisan serve`
2. Проверьте, что сервер на http://localhost:8000
3. Откройте http://localhost:8000/up в браузере

---

### Проблема: CORS ошибки

**Симптомы:**
```
Access to fetch at 'http://localhost:8000/api/...' from origin
'http://localhost:3000' has been blocked by CORS policy
```

**Решение:**
1. Проверьте `kfa-backend/kfa-api/config/cors.php`:
   ```php
   'allowed_origins' => [
       'http://localhost:3000',
       'http://127.0.0.1:3000',
   ],
   ```

2. Очистите кэш конфигурации:
   ```bash
   php artisan config:clear
   php artisan config:cache
   ```

---

### Проблема: Токены не работают

**Симптомы:**
```
{"message":"Unauthenticated."}
```

**Решение:**

1. **Проверьте наличие токена:**
   ```javascript
   localStorage.getItem('auth_token')
   ```

2. **Проверьте заголовок запроса:**
   - В DevTools → Network → Headers
   - Должен быть: `Authorization: Bearer {token}`

3. **Проверьте БД:**
   ```sql
   SELECT * FROM personal_access_tokens ORDER BY created_at DESC LIMIT 5;
   ```

4. **Проверьте срок действия:**
   - Токены живут 24 часа (1440 минут)
   - После этого нужно войти заново

---

### Проблема: База данных не создана

**Симптомы:**
```
SQLSTATE[HY000] [1049] Unknown database 'kfa_database'
```

**Решение:**
1. Создайте базу данных:
   ```sql
   CREATE DATABASE kfa_database CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. Проверьте `.env`:
   ```
   DB_DATABASE=kfa_database
   DB_USERNAME=root
   DB_PASSWORD=
   ```

3. Запустите миграции:
   ```bash
   php artisan migrate
   ```

---

### Проблема: Слабый пароль принимается

**Симптомы:**
Пароль `password` или `12345678` регистрируется успешно

**Решение:**
1. Проверьте файл `AuthController.php`:
   - Должно быть: `'regex:/^(?=.*[A-Za-z])(?=.*\d).+$/'`

2. Очистите кэш:
   ```bash
   php artisan config:clear
   php artisan route:clear
   ```

---

## 📊 КОНТРОЛЬНЫЙ СПИСОК

Пройдите по списку и отметьте:

- [ ] Backend запущен и доступен на http://localhost:8000
- [ ] База данных создана и миграции выполнены
- [ ] Слабый пароль `password` **отклоняется**
- [ ] Слабый пароль `12345678` **отклоняется**
- [ ] Короткий пароль `pass12` **отклоняется**
- [ ] Сильный пароль `MyPass2024` **принимается**
- [ ] Регистрация создает пользователя в БД
- [ ] После регистрации возвращается токен
- [ ] Вход с правильными данными работает
- [ ] Вход с неверными данными отклоняется
- [ ] Токен сохраняется в localStorage
- [ ] `/api/user` с токеном возвращает данные
- [ ] `/api/user` без токена возвращает 401
- [ ] Выход удаляет токен из БД
- [ ] После выхода токен не работает
- [ ] Перезагрузка страницы сохраняет авторизацию

**Если все пункты отмечены - система работает корректно! ✅**

---

## 🎉 СЛЕДУЮЩИЕ ШАГИ

После успешного тестирования:

1. **Удалите тестовых пользователей** (если создавали через seeders)
2. **Настройте production конфигурацию** (см. AUTH-SYSTEM-IMPROVED.md)
3. **Добавьте опциональные функции:**
   - Email verification
   - Password reset
   - 2FA/MFA

4. **Разверните на production:**
   - Обновите CORS origins
   - Настройте HTTPS
   - Не запускайте dev seeders

---

**Создано:** 29.10.2024
**Статус:** Готово к тестированию
**Документация:** AUTH-SYSTEM-IMPROVED.md
