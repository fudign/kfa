# Инструкции по Исправлению Загрузки Изображений

**Дата**: 2025-11-12
**Статус**: 🔧 ТРЕБУЕТ НАСТРОЙКИ SUPABASE

---

## 🎯 Проблемы Выявленные

1. ❌ **Неправильный ключ Supabase** - Использовался `anon` key вместо `service_role` key
2. ❌ **Неправильный endpoint** - Добавлялся лишний `/storage/v1`
3. ⚠️ **Bucket может не существовать** - Нужно проверить наличие bucket "media" в Supabase

---

## ✅ Исправления Применённые

### 1. Исправлена Конфигурация Supabase

**Файл: `kfa-backend/kfa-api/config/filesystems.php` (строки 63-71)**

```php
'supabase' => [
    'driver' => 'supabase',
    'key' => env('SUPABASE_SERVICE_ROLE_KEY'),  // ← Используем service role key
    'url' => env('SUPABASE_URL'),
    'endpoint' => env('SUPABASE_URL'),  // ← Адаптер автоматически добавит /storage/v1
    'bucket' => env('SUPABASE_STORAGE_BUCKET', 'media'),
    'visibility' => 'public',
    'throw' => false,
],
```

**Что изменилось:**

- ✅ Заменили `SUPABASE_KEY` на `SUPABASE_SERVICE_ROLE_KEY` для полных прав доступа
- ✅ Убрали дублирование `/storage/v1` из endpoint
- ✅ Установили `throw => false` для production

---

## 🔧 Критические Шаги Которые Нужно Выполнить

### Шаг 1: Проверить Supabase Bucket

#### 1.1 Войти в Supabase Dashboard

```
https://supabase.com/dashboard
```

#### 1.2 Открыть Проект

- Project: `eofneihisbhucxcydvac`
- URL: https://eofneihisbhucxcydvac.supabase.co

#### 1.3 Перейти в Storage

- В боковом меню найти **"Storage"**
- Проверить список buckets

#### 1.4 Создать Bucket "media" (если не существует)

Если bucket "media" НЕ существует:

1. Нажмите **"New bucket"**
2. Заполните:
   - **Name**: `media`
   - **Public bucket**: ✅ **Включить** (для публичного доступа к изображениям)
   - **Allowed MIME types**: Оставить пустым (разрешить все)
   - **File size limit**: `5MB` (или больше если нужно)
3. Нажмите **"Create bucket"**

#### 1.5 Настроить Policies для Bucket

**ВАЖНО**: Bucket должен разрешать:

- ✅ **SELECT** (чтение) - для всех
- ✅ **INSERT** (запись) - для authenticated users или service_role
- ✅ **UPDATE** (обновление) - для authenticated users или service_role
- ✅ **DELETE** (удаление) - для authenticated users или service_role

**Настройка Policies:**

1. Откройте bucket "media"
2. Перейдите в **"Policies"**
3. Создайте политику для публичного чтения:

```sql
-- Политика 1: Публичное чтение
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING ( bucket_id = 'media' );

-- Политика 2: Аутентифицированная запись
CREATE POLICY "Authenticated users can upload"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'media' AND
  auth.role() = 'authenticated'
);

-- Политика 3: Service role full access
CREATE POLICY "Service role has full access"
ON storage.objects
FOR ALL
USING ( auth.jwt() ->> 'role' = 'service_role' );
```

---

### Шаг 2: Проверить Environment Variables

**Файл: `kfa-backend/kfa-api/.env`**

Убедитесь что есть все переменные:

```env
# Supabase Configuration
SUPABASE_URL=https://eofneihisbhucxcydvac.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...  # Service role key!
SUPABASE_STORAGE_BUCKET=media

# Filesystem
FILESYSTEM_DISK=supabase
```

**⚠️ ВАЖНО**:

- Убедитесь что `SUPABASE_SERVICE_ROLE_KEY` содержит **service_role** key, НЕ anon key
- Service role key начинается с `eyJ...` и содержит `"role":"service_role"`

---

### Шаг 3: Перезапустить Backend

После изменения `.env` и `filesystems.php`:

```bash
# Остановить текущий сервер (Ctrl+C в терминале где запущен)
# Затем запустить заново:
cd kfa-backend/kfa-api
php artisan serve --port=8000
```

---

### Шаг 4: Тестировать Загрузку

#### Вариант 1: Через Frontend

1. Откройте `http://localhost:3000/dashboard/news`
2. Нажмите **"Добавить новость"**
3. В поле изображения нажмите **"Выбрать"**
4. Нажмите **"Загрузить"**
5. Выберите изображение (JPG, PNG, GIF, WebP, SVG - до 5MB)
6. Дождитесь загрузки

**Ожидаемый результат:**

- ✅ Изображение загружается
- ✅ Появляется в списке медиа
- ✅ Отображается превью
- ✅ URL начинается с `https://eofneihisbhucxcydvac.supabase.co/object/public/media/`

#### Вариант 2: Через Тестовый Скрипт

```bash
php test-upload.php
```

**Ожидаемый результат:**

```
✅ SUCCESS: Path is valid!
Path: 2025/11/12/abc123-image.png
URL: https://eofneihisbhucxcydvac.supabase.co/object/public/media/2025/11/12/abc123-image.png
```

---

## 🐛 Troubleshooting

### Проблема 1: Path все еще "0" или false

**Причины:**

- Bucket "media" не существует в Supabase
- Bucket policies не разрешают запись
- Service role key неправильный

**Решение:**

1. Проверить bucket существует (Шаг 1)
2. Проверить policies (Шаг 1.5)
3. Проверить `.env` содержит правильный service_role key

### Проблема 2: Error "Unable to write file"

**Причины:**

- Bucket не существует
- Policies запрещают запись
- Неправильный API key

**Решение:**

1. Создать bucket (Шаг 1.4)
2. Настроить policies (Шаг 1.5)
3. Использовать service_role key в `.env`

### Проблема 3: Изображения не отображаются

**Причины:**

- Bucket не публичный
- Policies не разрешают чтение
- CORS не настроен

**Решение:**

1. Сделать bucket публичным
2. Добавить policy для публичного чтения
3. Настроить CORS в Supabase:
   - Settings → API → CORS
   - Allowed origins: `*` или `http://localhost:3000, http://localhost:8000`

### Проблема 4: Error "endpoint is not specified"

**Причина:**

- Старая конфигурация filesystems.php

**Решение:**

- Убедитесь что `filesystems.php` обновлён (см. раздел "Исправления Применённые")

---

## 📊 Проверочный Список

Перед тем как тестировать загрузку, убедитесь что:

- [ ] ✅ Bucket "media" создан в Supabase Storage
- [ ] ✅ Bucket настроен как **Public**
- [ ] ✅ Policies разрешают чтение (SELECT) для всех
- [ ] ✅ Policies разрешают запись (INSERT, UPDATE, DELETE) для service_role
- [ ] ✅ `.env` содержит `SUPABASE_SERVICE_ROLE_KEY` (НЕ anon key!)
- [ ] ✅ `FILESYSTEM_DISK=supabase` в `.env`
- [ ] ✅ `filesystems.php` использует `SUPABASE_SERVICE_ROLE_KEY`
- [ ] ✅ `filesystems.php` endpoint = `SUPABASE_URL` (без `/storage/v1`)
- [ ] ✅ Backend перезапущен после изменений

---

## 🎉 После Успешной Настройки

Когда загрузка заработает:

### Структура Файлов в Supabase:

```
media/                                    ← Bucket
└── 2025/                                 ← Год
    └── 11/                               ← Месяц
        └── 12/                           ← День
            ├── abc123...xyz.png          ← Оригинал
            ├── abc123...xyz-thumbnail.png
            ├── abc123...xyz-small.png
            ├── abc123...xyz-medium.png
            └── abc123...xyz-large.png
```

### URL Формат:

```
https://eofneihisbhucxcydvac.supabase.co/object/public/media/2025/11/12/abc123...xyz.png
```

### Database Запись:

```sql
id: 1
filename: "image.png"
path: "2025/11/12/abc123...xyz.png"  ← Должен быть путь, НЕ "0"!
disk: "supabase"
url: "https://eofneihisbhucxcydvac.supabase.co/object/public/media/2025/11/12/abc123...xyz.png"
```

---

## 📝 Следующие Шаги

После того как загрузка изображений заработает:

1. ✅ Тестировать создание новостей с изображениями
2. ✅ Проверить отображение изображений на фронтенде
3. ✅ Проверить MediaPicker показывает загруженные изображения
4. ✅ Создать несколько тестовых новостей

---

**Важно**: Основная проблема в том, что **bucket "media" скорее всего не существует в Supabase**. Создайте его по инструкциям выше!

_Обновлено: 2025-11-12_
_Все изменения конфигурации уже применены в коде_
_Осталось только создать bucket в Supabase Dashboard_
