# План улучшения агентной разработки для проекта KFA

## Анализ текущего состояния

### Что уже есть в проекте KFA:

1. **Application Layer** (Готово):
   - `kfa-website/` - React/Vite фронтенд
   - `kfa-backend/` - PHP Laravel API
   - `docs/` - Документация проекта

2. **Agent Tools** (Частично реализовано):
   - `agent-tools/db/` - Статус БД, миграции, backup
   - `agent-tools/deploy/` - Health check, verify-env, build
   - `agent-tools/test/` - E2E и unit тесты
   - `agent-tools/media/` - Работа с Supabase медиа

3. **BMAD система** (Готово):
   - `bmad/` - BMAD Core модули
   - `.claude/commands/` - Slash команды

### Что отсутствует (по сравнению с tac-8):

1. **ADW (AI Developer Workflows)** - Отсутствует
2. **Specs директория** - Нет структурированных планов
3. **Observability система** - Нет мониторинга агентов
4. **Agents output директория** - Нет логов выполнения
5. **Workflow automation** - Нет автоматизации задач

---

## Рекомендации по улучшению

### 1. Создать ADW структуру (AI Developer Workflows)

**Приоритет:** ВЫСОКИЙ

Создать директорию `adws/` для оркестрации агентов:

```
adws/
├── adw_modules/              # Базовые модули
│   ├── agent.py             # Ядро выполнения Claude Code
│   ├── data_models.py       # Типы данных
│   └── utils.py             # Утилиты
├── adw_prompt.py            # Прямое выполнение промптов
├── adw_slash_command.py     # Выполнение slash команд
└── adw_chore_implement.py   # Планирование + реализация
```

**Преимущества:**
- Программная оркестрация агентов
- Возможность retry логики
- Сохранение всех outputs для отладки
- Масштабирование через compute

**Как применить к KFA:**
1. Создать `adws/adw_modules/agent.py` - адаптировать из tac-8 примера
2. Добавить `adw_chore_implement.py` для workflow планирование→реализация
3. Настроить uv для single-file scripts (уже есть shebang в примерах)

---

### 2. Улучшить Agent Tools

**Приоритет:** СРЕДНИЙ

Добавить недостающие инструменты для KFA специфики:

```bash
# Новые инструменты для KFA:
agent-tools/
├── supabase/
│   ├── test-connection.js    # Тест подключения к Supabase
│   ├── run-migrations.js     # Запуск SQL миграций
│   └── check-buckets.js      # Проверка storage buckets
├── railway/
│   ├── deploy-status.js      # Статус деплоя на Railway
│   └── check-logs.js         # Получение логов
└── vercel/
    ├── deploy-frontend.js    # Деплой на Vercel
    └── get-deployment.js     # Статус деплоя
```

**Примеры использования:**
```bash
# Проверить Supabase
node agent-tools/supabase/test-connection.js > supabase-status.json

# Проверить Railway деплой
node agent-tools/railway/deploy-status.js > railway-status.json

# Деплой на Vercel
node agent-tools/vercel/deploy-frontend.js --project=kfa-website
```

---

### 3. Создать Specs директорию

**Приоритет:** ВЫСОКИЙ

Структурированные спецификации для агентов:

```
specs/
├── chore-{id}-{name}.md      # Планы задач
├── feature-{name}.md         # Спецификации фич
└── deep_specs/               # Комплексные архитектурные спеки
    └── architecture.md
```

**Формат spec файла:**
```markdown
# Chore: {Title}

## Context
[Описание контекста]

## Objective
[Цель задачи]

## Implementation Plan
1. [ ] Step 1
2. [ ] Step 2

## Files to Modify
- kfa-website/src/...
- kfa-backend/app/...

## Tests
- [ ] Unit tests
- [ ] E2E tests
```

**Интеграция с BMAD:**
- Использовать существующие `.claude/commands/` для создания specs
- Агенты читают specs и выполняют по плану

---

### 4. Настроить Observability (опционально)

**Приоритет:** НИЗКИЙ (для продвинутого использования)

Если нужен мониторинг работы агентов:

1. Скопировать `.claude/hooks/` из tac-8 app3
2. Настроить `send_event.py` для отправки событий
3. Поднять observability server (опционально)

**Быстрый старт без сервера:**
```bash
# Просто логировать в файлы
.claude/hooks/
├── pre_tool_use.py      # Логирует перед выполнением
├── post_tool_use.py     # Логирует после выполнения
└── logs/                # Директория с логами
```

---

### 5. Организовать Agents Output

**Приоритет:** СРЕДНИЙ

Создать структуру для outputs агентов:

```
agents/
└── {adw_id}/              # Уникальный ID для каждого запуска
    ├── planner/           # Outputs агента планирования
    │   ├── cc_raw_output.jsonl
    │   ├── cc_final_object.json
    │   └── custom_summary_output.json
    ├── builder/           # Outputs агента реализации
    │   └── ...
    └── workflow_summary.json
```

**Преимущества:**
- Полная история выполнения
- Отладка failed runs
- Метрики производительности

---

## План внедрения

### Фаза 1: Minimum Viable ADW (1-2 часа)

1. **Создать базовую ADW структуру:**
   ```bash
   mkdir -p adws/adw_modules
   mkdir -p specs
   mkdir -p agents
   ```

2. **Скопировать agent.py из tac-8:**
   - Адаптировать пути для KFA
   - Настроить под Windows (если нужно)

3. **Создать первый workflow:**
   - `adw_prompt.py` - для ad-hoc промптов
   - Тест: выполнить простую задачу

4. **Создать первый spec:**
   - Выбрать простую задачу (например, добавить logging)
   - Создать `specs/chore-{id}-add-logging.md`

### Фаза 2: Улучшение Agent Tools (2-3 часа)

1. **Добавить Supabase инструменты:**
   - `agent-tools/supabase/test-connection.js`
   - Использовать @supabase/supabase-js

2. **Добавить Railway/Vercel инструменты:**
   - Базовые проверки статуса
   - Интеграция с API

3. **Обновить QUICK-REFERENCE.md:**
   - Добавить новые команды
   - Примеры использования

### Фаза 3: Workflow Automation (3-4 часа)

1. **Создать `adw_chore_implement.py`:**
   - Планирование через `/chore`
   - Автоматическая реализация через `/implement`

2. **Интеграция с git:**
   - Создание веток
   - Коммиты с ADW ID

3. **Создать примеры workflows:**
   - Добавление новой фичи
   - Исправление бага
   - Обновление документации

---

## Примеры использования для KFA

### Пример 1: Добавление новой фичи

```bash
# 1. Запустить workflow планирование + реализация
./adws/adw_chore_implement.py "Add user profile photo upload to KFA"

# Результат:
# - Создан specs/chore-abc12345-user-photo-upload.md
# - Код реализован в kfa-website/src/
# - Outputs сохранены в agents/abc12345/
```

### Пример 2: Проверка состояния проекта

```bash
# Запустить все проверки
node agent-tools/db/status.js > results/db.json
node agent-tools/supabase/test-connection.js > results/supabase.json
node agent-tools/deploy/health-check.js --url=https://kfa-website.vercel.app > results/frontend.json

# Агрегировать результаты
cat results/*.json | jq -s 'map({name: .timestamp, success: .success})'
```

### Пример 3: Автоматизация деплоя

```bash
#!/bin/bash
# agent-tools/scripts/deploy-kfa.sh

echo "Deploying KFA..."

# 1. Проверить состояние
node agent-tools/db/status.js || exit 1

# 2. Запустить тесты
node agent-tools/test/run-e2e.js || exit 1

# 3. Build frontend
cd kfa-website && npm run build || exit 1

# 4. Deploy to Vercel
node agent-tools/vercel/deploy-frontend.js --project=kfa-website

# 5. Health check
sleep 10
node agent-tools/deploy/health-check.js --url=https://kfa-website.vercel.app

echo "Deploy complete!"
```

---

## Ключевые принципы

### 1. Separatio of Concerns
- **Agentic Layer**: adws/, specs/, .claude/, agents/
- **Application Layer**: kfa-website/, kfa-backend/, docs/

### 2. Composability
- Инструменты выводят JSON
- Workflows комбинируют инструменты
- Агенты используют templates (.claude/commands/)

### 3. Observability
- Все outputs сохраняются
- ADW ID для трекинга
- Session ID от Claude Code

### 4. Scalability
- Параллельное выполнение агентов
- Retry логика для надежности
- Worktrees для изоляции (опционально)

---

## Быстрый старт

### Минимальная настройка (15 минут):

1. **Создать директории:**
   ```bash
   mkdir -p adws/adw_modules
   mkdir -p specs
   mkdir -p agents
   ```

2. **Скопировать agent.py из tac-8:**
   ```bash
   cp tac-8/tac8_app1__agent_layer_primitives/adws/adw_modules/agent.py adws/adw_modules/
   ```

3. **Создать .env с настройками:**
   ```bash
   ANTHROPIC_API_KEY=your_key
   CLAUDE_CODE_PATH=claude
   ```

4. **Создать первый ADW скрипт:**
   ```bash
   cp tac-8/tac8_app1__agent_layer_primitives/adws/adw_prompt.py adws/
   chmod +x adws/adw_prompt.py
   ```

5. **Тест:**
   ```bash
   ./adws/adw_prompt.py "List all files in kfa-website/src"
   ```

---

## Ресурсы

### Документация
- [Agent Tools Guide](AGENT-TOOLS-GUIDE.md)
- [BMAD Core](bmad/core/)
- [tac-8 Examples](tac-8/)

### Примеры из tac-8
- **app1**: Agent Layer Primitives - базовые ADW
- **app2**: Multi Agent Todone - задачи и автоматизация
- **app3**: Observability - мониторинг агентов

### Следующие шаги
1. Начать с Фазы 1 (Minimum Viable ADW)
2. Создать первый spec и протестировать workflow
3. Постепенно добавлять инструменты по мере необходимости
4. Масштабировать через параллельных агентов

---

## Выводы

Проект KFA уже имеет хорошую базу:
- ✅ Agent Tools (частично)
- ✅ BMAD система
- ✅ Документация

Добавление ADW структуры позволит:
- 🚀 Программная оркестрация агентов
- 🔄 Автоматизация workflows
- 📊 Полная observability
- ⚡ Масштабирование через compute

**Рекомендуемый подход:** Начать с Minimum Viable ADW (Фаза 1), протестировать на реальной задаче, затем постепенно добавлять функциональность.
