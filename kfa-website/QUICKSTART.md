# 🚀 Быстрый старт - KFA Website

## Пошаговая инструкция по запуску

### 1. Установка зависимостей

```bash
cd kfa-website
npm install
```

Это установит все необходимые пакеты (~5-10 минут при первом запуске).

### 2. Установка дополнительных shadcn/ui компонентов

Базовые компоненты Button и Card уже включены. Для добавления других:

```bash
# Установка shadcn/ui CLI (если еще не установлен)
npx shadcn-ui@latest init

# Добавление компонентов по необходимости
npx shadcn-ui@latest add input
npx shadcn-ui@latest add form
npx shadcn-ui@latest add select
npx shadcn-ui@latest add table
npx shadcn-ui@latest add tabs
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add dropdown-menu
```

### 3. Запуск dev-сервера

```bash
npm run dev
```

Сайт откроется автоматически на `http://localhost:3000`

### 4. Проверка работы

После запуска вы увидите:
- ✅ Hero секцию с Aceternity Spotlight эффектом
- ✅ Анимированный заголовок с градиентом
- ✅ 4 метрики с иконками
- ✅ Адаптивный дизайн (проверьте на mobile)

## 📦 Структура стартера

### Что уже настроено:

✅ **Конфигурация**
- Vite с оптимизациями
- TypeScript
- TailwindCSS с кастомной темой КФА
- ESLint + Prettier
- PWA support

✅ **UI Компоненты**
- shadcn/ui Button и Card
- Aceternity Spotlight и Hero
- Кастомные цвета и типографика

✅ **i18n**
- Поддержка RU/KY/EN
- Базовые переводы для hero секции

✅ **Утилиты**
- cn() для классов
- formatCurrency, formatDate, formatNumber
- debounce, truncate, getInitials

### Что нужно добавить:

#### Обязательно:
1. **Навигация** - Header с меню
2. **Футер** - Footer с контактами
3. **Страницы** - About, Membership, etc.
4. **Формы** - Контактная форма, регистрация
5. **API интеграция** - Backend endpoints

#### Опционально:
6. **Аутентификация** - NextAuth.js / Clerk / Supabase
7. **Дашборды** - Recharts графики
8. **Таблицы** - TanStack Table для членов
9. **Календарь** - События и мероприятия
10. **CMS** - Управление контентом

## 🎨 Кастомизация

### Изменение цветов

Откройте `tailwind.config.js` и измените цвета в секции `colors`:

```js
primary: {
  500: '#ВАШ_ЦВЕТ', // Основной синий
}
accent: {
  500: '#ВАШ_ЦВЕТ', // Золотой
}
```

### Изменение шрифтов

В `index.html` замените ссылку на Google Fonts:

```html
<link href="https://fonts.googleapis.com/css2?family=ВАШИ_ШРИФТЫ" rel="stylesheet" />
```

Затем обновите `tailwind.config.js`:

```js
fontFamily: {
  sans: ['ВАШ_ШРИФТ', 'system-ui', 'sans-serif'],
}
```

### Добавление новых страниц

1. Создайте файл в `src/pages/public/NewPage.tsx`
2. Добавьте роут в `src/app/App.tsx`:

```tsx
<Route path="/new-page" element={<NewPage />} />
```

## 🔧 Распространенные проблемы

### Problem: `npm install` долго устанавливается
**Решение**: Используйте pnpm или yarn для ускорения:
```bash
pnpm install  # или
yarn install
```

### Problem: Ошибка импорта компонентов
**Решение**: Проверьте alias в `vite.config.ts` и `tsconfig.json`

### Problem: TailwindCSS классы не работают
**Решение**:
1. Проверьте, что импортирован `@/styles/globals.css`
2. Перезапустите dev-сервер

### Problem: i18n переводы не загружаются
**Решение**: Убедитесь что файлы в `public/locales/{lang}/*.json` существуют

## 📱 Тестирование на мобильных

### Chrome DevTools:
1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Выберите iPhone/Android
3. Проверьте адаптивность

### Реальное устройство:
1. Найдите локальный IP: `ipconfig` (Windows) или `ifconfig` (Mac/Linux)
2. Откройте на телефоне: `http://YOUR_IP:3000`

## 🚢 Деплой

### Vercel (рекомендуется):
```bash
npm install -g vercel
vercel
```

### Netlify:
```bash
npm run build
netlify deploy --prod --dir=dist
```

### Свой сервер:
```bash
npm run build
# Загрузите папку dist/ на сервер
# Настройте nginx/apache для SPA
```

## 📚 Дополнительные ресурсы

- [Vite Documentation](https://vitejs.dev)
- [React Documentation](https://react.dev)
- [shadcn/ui Components](https://ui.shadcn.com)
- [TailwindCSS Docs](https://tailwindcss.com)
- [Framer Motion](https://www.framer.com/motion/)

## 🆘 Нужна помощь?

- Создайте issue в репозитории
- Обратитесь к техлиду команды
- Проверьте README.md для деталей

---

**Удачи в разработке! 🎉**
