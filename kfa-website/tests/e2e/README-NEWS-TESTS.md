# E2E Тесты для управления новостями

Комплексные End-to-End тесты для страницы управления новостями (`/dashboard/news`)

## 📋 Содержание тестов

### 1. Основные функции (2 теста)

- ✅ Отображение страницы управления новостями
- ✅ Открытие формы создания новости

### 2. CRUD операции (4 теста)

- ✅ Создание новости с полными данными
- ✅ Автоматическая генерация slug из заголовка
- ✅ Редактирование существующей новости
- ✅ Удаление новости с подтверждением

### 3. Работа с изображениями (3 теста)

- ✅ Добавление изображения через URL
- ✅ Открытие медиа-пикера для выбора изображения
- ✅ Очистка изображения

### 4. Валидация и фильтрация (5 тестов)

- ✅ Валидация обязательных полей
- ✅ Валидация минимальной длины заголовка
- ✅ Поиск новостей по заголовку
- ✅ Фильтрация новостей по статусу
- ✅ Сброс фильтров

### 5. Дополнительные функции (5 тестов)

- ✅ Переключение статуса Featured
- ✅ Отмена создания новости
- ✅ Закрытие формы по крестику
- ✅ Отображение пагинации при большом количестве новостей
- ✅ Изменение статуса новости

### 6. Контроль доступа (1 тест)

- ✅ Перенаправление на login без авторизации

### 7. Обработка ошибок (2 теста)

- ✅ Обработка ошибок сети при загрузке
- ✅ Корректное отображение пустого списка

**Всего: 22 комплексных E2E теста**

## 🚀 Быстрый запуск

### Метод 1: Автоматический запуск (рекомендуется)

Этот метод автоматически освобождает порт, запускает dev сервер и выполняет тесты:

```bash
cd kfa-website
npm run test:e2e:news
```

### Метод 2: Ручной запуск

```bash
# 1. Освободить порт 3000 (если занят)
npm run kill-port 3000

# 2. Запустить dev сервер (в отдельном терминале)
npm run dev

# 3. В другом терминале запустить тесты
npm run test:e2e -- cms-news.spec.ts
```

## 🔧 Дополнительные опции

### Запуск тестов в определенном браузере

```bash
# Только Chromium
npm run test:e2e -- cms-news.spec.ts --project=chromium

# Только Firefox
npm run test:e2e -- cms-news.spec.ts --project=firefox

# Только WebKit
npm run test:e2e -- cms-news.spec.ts --project=webkit
```

### Запуск с UI режимом

```bash
npm run test:e2e -- cms-news.spec.ts --ui
```

### Запуск с отладкой

```bash
npm run test:e2e -- cms-news.spec.ts --debug
```

### Запуск в headed режиме (видимый браузер)

```bash
npm run test:e2e -- cms-news.spec.ts --headed
```

### Запуск конкретного теста

```bash
npm run test:e2e -- cms-news.spec.ts -g "должен создать новую новость"
```

## 📝 Освобождение портов

Если порт 3000 занят другим процессом:

```bash
# Освободить порт 3000
npm run kill-port 3000

# Освободить несколько портов
npm run kill-port 3000 3001
```

## 🛠️ Требования

1. **Node.js** - версия 18 или выше
2. **Backend API** - должен быть запущен и доступен
3. **База данных** - должна быть настроена с тестовыми данными

## 📊 Проверка состояния

Перед запуском тестов убедитесь:

1. ✅ Backend API работает корректно
2. ✅ База данных содержит тестовые данные (пользователи, роли)
3. ✅ Порт 3000 свободен
4. ✅ Установлены все зависимости (`npm install`)

## 🔍 Отладка тестов

### Просмотр отчетов

После выполнения тестов Playwright генерирует отчет:

```bash
npx playwright show-report
```

### Генерация trace файлов

Для детальной отладки можно записать trace:

```bash
npm run test:e2e -- cms-news.spec.ts --trace on
```

Затем посмотреть trace:

```bash
npx playwright show-trace test-results/[test-name]/trace.zip
```

### Скриншоты и видео

При падении тестов автоматически создаются скриншоты в папке `test-results/`

## 📁 Структура файлов

```
kfa-website/
├── tests/
│   └── e2e/
│       ├── cms-news.spec.ts          # Тесты для новостей
│       └── README-NEWS-TESTS.md      # Эта документация
├── scripts/
│   ├── kill-port.cjs                 # Скрипт освобождения портов
│   └── run-e2e-tests.cjs             # Скрипт автозапуска тестов
└── playwright.config.ts              # Конфигурация Playwright
```

## 🐛 Решение проблем

### Порт 3000 занят

```bash
npm run kill-port 3000
```

### Тесты падают с timeout

Увеличьте таймаут в `playwright.config.ts`:

```typescript
timeout: 60 * 1000, // 60 секунд
```

### Backend не отвечает

Проверьте:

1. Запущен ли Laravel сервер
2. Правильные ли настройки CORS
3. Доступна ли база данных

### Ошибки авторизации

Убедитесь что:

1. В базе данных есть пользователь `admin@kfa.kg`
2. У пользователя есть права доступа к контенту
3. Система быстрой авторизации работает корректно

## 📈 CI/CD интеграция

Для запуска в CI/CD добавьте в pipeline:

```yaml
test-e2e:
  script:
    - npm install
    - npx playwright install --with-deps
    - npm run kill-port 3000
    - npm run dev & # Запуск в фоне
    - sleep 10 # Ожидание запуска сервера
    - npm run test:e2e -- cms-news.spec.ts --project=chromium
```

## 🔗 Полезные ссылки

- [Playwright Documentation](https://playwright.dev/)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [API Reference](https://playwright.dev/docs/api/class-test)

## 📝 Changelog

### v1.0.0 (2025-10-28)

- ✨ Создан полный набор E2E тестов для управления новостями
- ✨ Добавлена автоматическая генерация slug
- ✨ Добавлены утилиты для управления портами
- ✨ Создан скрипт автоматического запуска тестов
