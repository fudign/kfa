# 🚀 Полная настройка системы новостей и медиа

## Что нужно сделать

Для работы системы управления новостями нужны:

1. ✅ Таблица `news` - для хранения новостей
2. ✅ Таблица `media` - для хранения информации о файлах
3. ✅ Storage bucket `media` - для хранения самих файлов
4. ✅ Права (permissions) для админа

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите: https://supabase.com/dashboard
2. Войдите в аккаунт
3. Выберите проект **kfa-production**

---

### Шаг 2: Создайте таблицу NEWS

#### 2.1 Откройте SQL Editor
- В левом меню нажмите **SQL Editor**
- Нажмите **New query**

#### 2.2 Выполните SQL для news
1. Откройте файл **`CREATE-NEWS-TABLE.sql`**
2. Скопируйте весь код (Ctrl+A, Ctrl+C)
3. Вставьте в SQL Editor (Ctrl+V)
4. Нажмите **Run** (или F5)

✅ **Результат:** Таблица news создана с 5 тестовыми новостями

---

### Шаг 3: Создайте таблицу MEDIA

#### 3.1 Откройте новый запрос
- В SQL Editor нажмите **New query**

#### 3.2 Выполните SQL для media
1. Откройте файл **`CREATE-MEDIA-TABLE.sql`**
2. Скопируйте весь код
3. Вставьте в SQL Editor
4. Нажмите **Run**

✅ **Результат:** Таблица media создана

---

### Шаг 4: Обновите права админа

#### 4.1 Откройте новый запрос
- В SQL Editor нажмите **New query**

#### 4.2 Добавьте права для медиа
1. Откройте файл **`UPDATE-ADMIN-MEDIA-PERMISSIONS.sql`**
2. Скопируйте весь код
3. Вставьте в SQL Editor
4. Нажмите **Run**

✅ **Результат:** Админ получил права media.view, media.upload, media.edit, media.delete

---

### Шаг 5: Создайте Storage Bucket

#### 5.1 Откройте Storage
- В левом меню Supabase Dashboard нажмите **Storage**

#### 5.2 Создайте bucket
1. Нажмите **"New bucket"** (вверху справа)
2. Введите данные:
   - **Name:** `media`
   - **Public bucket:** ✅ **ВКЛЮЧИТЕ ГАЛОЧКУ!** (это важно!)
3. Нажмите **"Create bucket"**

#### 5.3 Настройте политики безопасности

**Вариант A: Через UI**
1. Нажмите на bucket **"media"**
2. Вкладка **"Policies"**
3. Нажмите **"New policy"** для каждой политики:

**Политика 1:**
- Name: `Public can read files`
- Operations: SELECT ✅
- Roles: anon, authenticated
- USING: `true`

**Политика 2:**
- Name: `Authenticated users can upload`
- Operations: INSERT ✅
- Roles: authenticated
- WITH CHECK: `bucket_id = 'media'`

**Политика 3:**
- Name: `Users can delete own files`
- Operations: DELETE ✅
- Roles: authenticated
- USING: `bucket_id = 'media' AND auth.uid() = owner`

**Вариант B: Через SQL** (если UI не работает)

Выполните в SQL Editor:

```sql
CREATE POLICY "Anyone can read from media bucket"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'media');

CREATE POLICY "Authenticated users can upload to media bucket"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'media');

CREATE POLICY "Users can delete own files"
ON storage.objects FOR DELETE
TO authenticated
USING (bucket_id = 'media' AND auth.uid() = owner);
```

✅ **Результат:** Storage bucket создан и настроен

---

### Шаг 6: Проверка

#### 6.1 Проверьте в SQL Editor

Выполните этот запрос чтобы убедиться что всё создано:

```sql
-- Проверка таблицы news
SELECT 'news' as table_name, COUNT(*) as records FROM public.news
UNION ALL
-- Проверка таблицы media
SELECT 'media' as table_name, COUNT(*) as records FROM public.media
UNION ALL
-- Проверка прав админа
SELECT 'admin_permissions' as table_name,
       array_length(permissions, 1) as records
FROM public.profiles
WHERE role = 'admin'
LIMIT 1;
```

**Ожидаемый результат:**
```
table_name           | records
---------------------|--------
news                 | 5
media                | 1
admin_permissions    | 28
```

#### 6.2 Проверьте Storage

1. Storage → media
2. Bucket должен быть:
   - ✅ Создан
   - ✅ Public
   - ✅ С 3 политиками

---

### Шаг 7: Тестирование на сайте

#### 7.1 Откройте сайт
https://kfa-website.vercel.app/dashboard/news

#### 7.2 ОБЯЗАТЕЛЬНО перелогиньтесь!
1. Выйдите из системы (Logout)
2. Войдите снова с теми же данными
3. Это обновит токен с новыми правами

#### 7.3 Проверьте список новостей
Вы должны увидеть:
```
📰 Управление новостями

[+ Создать новость]  [🔍 Поиск]

┌─────────────────────────────────────────┐
│ ⭐ Добро пожаловать в систему...        │
│ 🟢 Published | Общее                   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Как работать с редактором новостей      │
│ 🟢 Published | Инструкции              │
└─────────────────────────────────────────┘

... и еще 3 новости
```

#### 7.4 Создайте тестовую новость с изображением

1. Нажмите **"+ Создать новость"**
2. Заполните форму:
   ```
   Заголовок: Тестовая новость с изображением
   Содержание: Проверка загрузки изображений
   Категория: Тест
   ```
3. **Загрузите изображение:**
   - Перетащите файл (drag & drop)
   - ИЛИ нажмите на зону загрузки и выберите файл
4. Дождитесь загрузки (появится превью)
5. Нажмите **"Сохранить как черновик"**

✅ Если всё работает - новость сохранится с изображением!

---

## ✅ Чеклист готовности

Отметьте что выполнено:

- [ ] SQL скрипт CREATE-NEWS-TABLE.sql выполнен
- [ ] SQL скрипт CREATE-MEDIA-TABLE.sql выполнен
- [ ] SQL скрипт UPDATE-ADMIN-MEDIA-PERMISSIONS.sql выполнен
- [ ] Storage bucket `media` создан
- [ ] Bucket `media` помечен как Public
- [ ] Политики для bucket созданы
- [ ] Перелогинились на сайте
- [ ] Видите список из 5 новостей
- [ ] Можете создать новость
- [ ] Можете загрузить изображение

---

## 🔧 Устранение проблем

### "Could not find the table 'public.news'"
❌ **Проблема:** Таблица news не создана
✅ **Решение:** Выполните CREATE-NEWS-TABLE.sql

### "Could not find the table 'public.media'"
❌ **Проблема:** Таблица media не создана
✅ **Решение:** Выполните CREATE-MEDIA-TABLE.sql

### "Bucket not found"
❌ **Проблема:** Storage bucket не создан
✅ **Решение:** Создайте bucket через Storage UI

### "new row violates row-level security policy"
❌ **Проблема:** Нет прав у админа
✅ **Решение:** Выполните UPDATE-ADMIN-MEDIA-PERMISSIONS.sql и перелогиньтесь

### Изображение не загружается (показывает broken image)
❌ **Проблема:** Bucket не public или нет политик
✅ **Решение:**
1. Storage → media → Configuration
2. Включите "Public bucket"
3. Создайте политику "Public can read files"

### Не вижу меню "Новости" в дашборде
❌ **Проблема:** Не перелогинились после обновления прав
✅ **Решение:** Выйдите и войдите снова

---

## 📊 Что было создано

### Таблицы в базе данных

**public.news** (5 записей)
- Хранит новости с заголовком, содержимым, статусом
- RLS политики для content.view, content.create и т.д.

**public.media** (1 placeholder запись)
- Хранит метаданные файлов (имя, размер, URL)
- RLS политики для media.view, media.upload и т.д.

### Storage bucket

**media** (public)
- Хранит сами файлы (изображения, документы)
- Политики для публичного чтения и загрузки

### Права админа (28 permissions)

- content.* (view, create, edit, delete, publish)
- media.* (view, upload, edit, delete)
- events.* (view, create, edit, delete)
- members.* (view, edit)
- partners.* (view, create, edit, delete)
- settings.* (view, edit)
- analytics.view
- users.* (view, manage)

---

## 🎉 Готово!

После выполнения всех шагов система полностью работоспособна:

✅ Можно просматривать новости
✅ Можно создавать новости
✅ Можно редактировать новости
✅ Можно загружать изображения
✅ Можно публиковать новости
✅ Можно удалять новости

**Попробуйте создать первую новость с изображением!**

https://kfa-website.vercel.app/dashboard/news

---

## 📞 Если нужна помощь

1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network tab (F12 → Network)
3. Убедитесь что вошли как admin
4. Попробуйте очистить кэш (Ctrl+Shift+R)
5. Проверьте все ли SQL скрипты выполнены
6. Проверьте что bucket создан и public
