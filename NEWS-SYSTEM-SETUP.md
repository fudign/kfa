# 📰 СИСТЕМА УПРАВЛЕНИЯ НОВОСТЯМИ - ИНСТРУКЦИЯ ПО ЗАПУСКУ

## ✅ ЧТО БЫЛО РЕАЛИЗОВАНО

### Backend (Laravel) ✅
- ✅ Модель News с полными связями (media, author)
- ✅ Полиморфная связь News ↔ Media через таблицу `mediable`
- ✅ NewsController с CRUD + управление медиа + модерация
- ✅ Request классы (StoreNewsRequest, UpdateNewsRequest) с валидацией
- ✅ NewsResource с полной сериализацией данных
- ✅ Миграции для связей news-media
- ✅ API routes для всех операций
- ✅ Поддержка статусов, модерации, избранного
- ✅ Фильтрация, поиск, пагинация
- ✅ Статистика по новостям

### Frontend (React + TypeScript) ✅
- ✅ TypeScript типы для News (types/news.ts)
- ✅ NewsService API client (services/api/news.ts)
- ✅ NewsManager страница уже существует
- ✅ Интеграция с существующим MediaManager

---

## 🚀 ШАГИ ДЛЯ ЗАПУСКА

### ШАГ 1: Запуск миграций Backend

```bash
cd kfa-backend/kfa-api

# Запустить новые миграции
php artisan migrate

# Если нужно пересоздать БД (ВНИМАНИЕ: удалит все данные!)
php artisan migrate:fresh --seed
```

**Созданные миграции:**
- `2025_10_27_000001_update_mediable_table.php` - добавляет `type`, `order`, timestamps
- `2025_10_27_000002_add_featured_image_id_to_news_table.php` - добавляет связь на главное изображение

### ШАГ 2: Проверка разрешений

Убедитесь, что у ролей есть необходимые разрешения:

```php
// Разрешения для работы с новостями
'content.view'    // Просмотр новостей
'content.create'  // Создание новостей
'content.update'  // Редактирование новостей
'content.delete'  // Удаление новостей
'media.view'      // Просмотр медиафайлов
'media.upload'    // Загрузка медиафайлов
```

Назначить разрешения роли можно через:
```bash
php artisan tinker

# Для роли admin
$admin = Role::where('name', 'admin')->first();
$admin->givePermissionTo(['content.view', 'content.create', 'content.update', 'content.delete']);
```

### ШАГ 3: Тестирование Backend API

```bash
# Получить список новостей
curl http://localhost:8000/api/news

# Получить статистику (требует аутентификацию)
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8000/api/news/stats

# Создать новость (требует аутентификацию)
curl -X POST http://localhost:8000/api/news \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовая новость",
    "content": "Содержимое тестовой новости",
    "status": "draft"
  }'
```

### ШАГ 4: Frontend - проверка зависимостей

```bash
cd kfa-website

# Проверить, что установлены все зависимости
npm install

# Запустить dev сервер
npm run dev
```

### ШАГ 5: Доступ к NewsManager

После запуска frontend откройте:
```
http://localhost:5173/dashboard/news
```

**Требуется:**
- Авторизация (логин/пароль)
- Права доступа (разрешение `content.view`)

---

## 📋 ДОСТУПНЫЕ API ENDPOINTS

### Публичные endpoints (без аутентификации)
```
GET    /api/news              - Список новостей
GET    /api/news/{id}         - Одна новость
```

### Защищенные endpoints (требуют аутентификации)

#### CRUD операции
```
POST   /api/news              - Создать новость
PUT    /api/news/{id}         - Обновить новость
DELETE /api/news/{id}         - Удалить новость
```

#### Управление медиа
```
POST   /api/news/{id}/media            - Прикрепить медиафайл
DELETE /api/news/{id}/media/{mediaId}  - Открепить медиафайл
PUT    /api/news/{id}/media/reorder    - Изменить порядок
```

#### Модерация (только админы)
```
POST   /api/news/{id}/approve          - Одобрить
POST   /api/news/{id}/reject           - Отклонить
POST   /api/news/{id}/toggle-featured  - Переключить "избранное"
POST   /api/news/{id}/publish          - Опубликовать
POST   /api/news/{id}/unpublish        - Снять с публикации
POST   /api/news/{id}/archive          - Архивировать
```

#### Статистика
```
GET    /api/news/stats        - Статистика по новостям
```

---

## 💡 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Создание новости с изображением

```typescript
// 1. Загрузить изображение через MediaManager
const media = await MediaService.upload(file, 'news');

// 2. Создать новость с изображением
const news = await NewsService.create({
  title: 'Новая статья',
  content: 'Содержимое статьи...',
  excerpt: 'Краткое описание',
  status: 'draft',
  featured: false,
  featured_image_id: media.id,
});
```

### Добавление галереи изображений

```typescript
// Загрузить несколько изображений
const images = await Promise.all(
  files.map(file => MediaService.upload(file, 'news-gallery'))
);

// Создать новость с галереей
const news = await NewsService.create({
  title: 'Статья с галереей',
  content: 'Содержимое...',
  status: 'draft',
  gallery_ids: images.map(img => img.id),
});
```

### Публикация новости

```typescript
// Изменить статус на "опубликовано"
await NewsService.publish(newsId);

// Или обновить вручную
await NewsService.update(newsId, {
  status: 'published',
  published_at: new Date().toISOString(),
});
```

---

## 🔧 НАСТРОЙКА И КАСТОМИЗАЦИЯ

### Добавление новых статусов

1. Обновите типы в `kfa-website/src/types/news.ts`:
```typescript
export type NewsStatus =
  | 'draft'
  | 'your_new_status'  // <-- добавить здесь
  | ...;
```

2. Обновите labels и colors там же

3. Обновите валидацию в `StoreNewsRequest.php`:
```php
'status' => 'nullable|in:draft,your_new_status,...',
```

### Изменение полей формы

Обновите `NewsFormData` в `types/news.ts`:
```typescript
export interface NewsFormData {
  title: string;
  // ... существующие поля
  your_new_field: string;  // <-- новое поле
}
```

И добавьте валидацию в Request классы.

### Настройка прав доступа

В `routes/api.php` измените middleware:
```php
Route::middleware(['auth:sanctum', 'permission:your_permission'])->group(function () {
    // ваши роуты
});
```

---

## 🎨 ИНТЕРФЕЙС NEWSMANAGER

Страница NewsManager (`/dashboard/news`) включает:

1. **Статистика** - карточки с цифрами (всего, опубликовано, черновики, на модерации)
2. **Фильтры** - поиск по названию, фильтр по статусу
3. **Таблица новостей** с колонками:
   - Изображение (thumbnail)
   - Заголовок
   - Автор
   - Статус
   - Дата публикации
   - Действия (редактировать, удалить)
4. **Форма создания/редактирования** - модальное окно
5. **Управление статусом** - кнопки для публикации, архивации

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Ошибка: "Table 'mediable' doesn't exist"
```bash
php artisan migrate
```

### Ошибка: "Permission denied"
Проверьте, что у пользователя есть разрешение `content.view` или выше.

### Изображения не отображаются
Проверьте:
```bash
# Создать символическую ссылку для storage
php artisan storage:link

# Проверить права доступа
chmod -R 775 storage/
chmod -R 775 bootstrap/cache/
```

### CORS ошибки
Обновите `config/cors.php`:
```php
'paths' => ['api/*', 'sanctum/csrf-cookie'],
'allowed_origins' => ['http://localhost:5173'],
```

---

## 📊 СТРУКТУРА ДАННЫХ

### Таблица `news`
```sql
- id
- title
- slug
- content
- excerpt
- image (устаревшее)
- status
- featured
- published_at
- author_id
- featured_image_id (FK на media.id)
- created_at
- updated_at
```

### Таблица `mediable` (полиморфная связь)
```sql
- media_id (FK на media.id)
- mediable_id (FK на news.id или др.)
- mediable_type ('App\Models\News' или др.)
- collection
- type ('featured' | 'gallery')
- order
- created_at
- updated_at
```

---

## ✨ ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

### Планировщик публикаций
Можно добавить команду в `app/Console/Kernel.php`:
```php
$schedule->call(function () {
    News::where('status', 'pending')
        ->where('published_at', '<=', now())
        ->update(['status' => 'published']);
})->everyMinute();
```

### Уведомления об изменении статуса
Создать Event и Listener:
```bash
php artisan make:event NewsStatusChanged
php artisan make:listener SendNewsStatusNotification
```

### Версионирование новостей
Установить пакет:
```bash
composer require spatie/laravel-activitylog
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- [Laravel Eloquent Relationships](https://laravel.com/docs/eloquent-relationships)
- [Spatie Laravel Permission](https://spatie.be/docs/laravel-permission)
- [React Query](https://tanstack.com/query/latest)
- [Radix UI](https://www.radix-ui.com/)

---

## 🎉 ГОТОВО!

Система управления новостями полностью готова к использованию. Теперь администраторы могут:

✅ Создавать и редактировать новости
✅ Загружать изображения (главное + галерея)
✅ Модерировать контент
✅ Публиковать/снимать с публикации
✅ Просматривать статистику
✅ Фильтровать и искать новости

Удачи! 🚀
