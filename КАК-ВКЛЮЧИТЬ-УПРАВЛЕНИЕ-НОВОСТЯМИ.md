# 🎯 Как включить управление новостями в КФА

## Проблема

После авторизации в дашборде не видно **"Управление новостями"** в левом сайдбаре.

## Причина

Пользователю не хватает прав доступа (`content.view` permission). Sidebar показывает пункт "Новости" только если у пользователя есть это право.

## Решение (3 шага - 5 минут)

### ШАГ 1: Создать таблицу profiles в Supabase

1. **Открой Supabase SQL Editor:**

   ```
   https://supabase.com/dashboard/project/eofneihisbhucxcydvac/sql
   ```

2. **Скопируй ВЕСЬ файл:** `supabase-auth-setup.sql` (в корне проекта)

3. **Вставь в SQL Editor и нажми "Run"** (Ctrl+Enter)

4. **Проверь успех:**

   ```sql
   SELECT * FROM public.profiles;
   ```

   Должна появиться таблица с 4 пользователями ✅

### ШАГ 2: Добавить права доступа пользователям

1. **В терминале запусти:**

   ```bash
   node add-admin-permissions.js
   ```

2. **Должно показать:**

   ```
   ✅ Found 4 profiles
   🔧 Updating permissions...
      Processing: admin@kfa.kg (admin)
      → Adding 20 permissions
      ✅ Updated successfully

   ✅ Now you can login and access:
      - Dashboard → Управление контентом → Новости
   ```

### ШАГ 3: Создать таблицу news в Supabase (опционально)

Этот шаг нужен, если хочешь использовать Supabase напрямую вместо Laravel API.

1. **Открой Supabase SQL Editor:**

   ```
   https://supabase.com/dashboard/project/eofneihisbhucxcydvac/sql
   ```

2. **Скопируй ВЕСЬ файл:** `supabase-news-table.sql` (в корне проекта)

3. **Вставь в SQL Editor и нажми "Run"**

4. **Проверь успех:**

   ```sql
   SELECT * FROM public.news;
   ```

   Должны появиться 3 тестовые новости ✅

---

## Проверка результата

1. **Обнови страницу дашборда:** F5 на http://localhost:3000/dashboard

2. **Проверь левый сайдбар:**

   ```
   Обзор
   Профиль
   Платежи
   Документы
   Сертификаты
   Обучение

   ═══ УПРАВЛЕНИЕ КОНТЕНТОМ ═══  ← Эта секция должна появиться!

   Новости        ← ✅ Должно быть видно!
   События
   Участники
   Медиафайлы
   Партнеры
   Настройки
   ```

3. **Кликни на "Новости"** → Должна открыться страница управления новостями

4. **Проверь функции:**
   - ✅ Список новостей (пустой или с данными из Laravel API)
   - ✅ Кнопка "Добавить новость"
   - ✅ Поиск и фильтры
   - ✅ Форма создания/редактирования
   - ✅ Выбор изображения (MediaPicker)

---

## Что работает после настройки

### ✅ В Dashboard (для авторизованных):

- **Управление новостями** - полный CRUD (Create, Read, Update, Delete)
- **Загрузка фото** - через MediaPicker с Supabase Storage
- **Редактирование контента** - textarea с валидацией
- **Статусы** - draft, published, archived
- **Избранные новости** - флаг featured
- **Поиск и фильтры** - по заголовку и статусу

### ✅ На главной странице (для всех):

- **NewsSection** - отображает новости из базы (после обновления компонента)
- **3 последние новости** - с изображениями и категориями
- **Кнопка "Все новости"** - ссылка на /news

---

## Права доступа по ролям

После выполнения `add-admin-permissions.js`:

### Admin (20 permissions):

```
content.view, content.create, content.edit, content.delete, content.publish
media.view, media.upload, media.edit, media.delete
events.view, events.create, events.edit, events.delete
members.view, members.edit
partners.view, partners.create, partners.edit, partners.delete
settings.view, settings.edit
```

### Editor (10 permissions):

```
content.view, content.create, content.edit, content.publish
media.view, media.upload, media.edit
events.view, events.create, events.edit
```

### Moderator (4 permissions):

```
content.view, content.edit
media.view
events.view, members.view
```

### Member (1 permission):

```
content.view
```

---

## Если что-то не работает

### "Profiles table not found"

→ Запусти SQL скрипт: `supabase-auth-setup.sql` в Supabase SQL Editor

### "No profiles found"

→ Создай пользователей: `node create-users-simple.js`

### "Sidebar не показывает Управление контентом"

→ Добавь permissions: `node add-admin-permissions.js`

### "Вход не работает"

→ Проверь что users созданы: https://supabase.com/dashboard/project/eofneihisbhucxcydvac/auth/users

### "NewsManager показывает 'У вас нет доступа'"

→ Перелогинься заново (logout → login), чтобы обновить permissions в сессии

---

## Быстрый старт (команды в порядке выполнения)

```bash
# 1. Открой Supabase SQL Editor и запусти:
#    supabase-auth-setup.sql

# 2. Добавь права доступа:
node add-admin-permissions.js

# 3. (Опционально) Создай таблицу news в Supabase:
#    supabase-news-table.sql

# 4. Перелогинься:
#    http://localhost:3000/auth/login
#    Email: admin@kfa.kg
#    Password: password

# 5. Открой дашборд:
#    http://localhost:3000/dashboard

# 6. Кликни: Управление контентом → Новости ✅
```

---

## Архитектура новостей

### Текущая реализация (Hybrid):

```
┌─────────────────────────────────────────────────┐
│  Frontend (React + Vite)                        │
│  - NewsManager.tsx (CRUD interface)             │
│  - NewsSection.tsx (Homepage display)           │
└─────────────────┬───────────────────────────────┘
                  │
                  ├─────────────────┬─────────────────┐
                  │                 │                 │
            ┌─────▼──────┐    ┌────▼────┐    ┌──────▼───────┐
            │ Laravel API │    │ Supabase│    │   Supabase   │
            │   (News)    │    │  Auth   │    │   Storage    │
            └─────┬──────┘    └─────────┘    └──────────────┘
                  │
            ┌─────▼──────┐
            │  SQLite    │ (local)
            │ PostgreSQL │ (production)
            └────────────┘
```

### Опции для новостей:

**Вариант A (текущий):** Laravel API + SQLite/PostgreSQL

- ✅ Готово и работает
- ✅ NewsManager уже использует newsAPI
- ✅ Миграции и модели готовы

**Вариант B (будущее):** Supabase direct access

- ⏳ Требует обновления NewsSection
- ⏳ Требует создания news таблицы в Supabase (скрипт готов!)
- ✅ Быстрее (no API roundtrip)
- ✅ Real-time updates

---

## Следующие шаги

После включения управления новостями:

1. ✅ **Проверить NewsManager** - создать/редактировать новость
2. ✅ **Обновить NewsSection** - загружать из API вместо hardcoded
3. ✅ **Добавить новости на главную** - интеграция с NewsSection
4. ✅ **Тестировать загрузку фото** - через MediaPicker
5. ✅ **Проверить на production** - Railway + Supabase

---

**Готово! Теперь у тебя полный CRUD новостей с загрузкой фото и отображением на главной странице!** 🎉
