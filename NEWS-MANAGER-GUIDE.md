# Руководство по Управлению Новостями

## 🎉 Реализовано!

Система управления новостями полностью готова к использованию на https://kfa-website.vercel.app/dashboard/news

---

## ✨ Новые Функции

### 1. Rich Text Editor для Контента

- **Панель форматирования** с кнопками для:
  - Заголовки (H1, H2, H3)
  - Жирный, курсив
  - Списки (маркированные и нумерованные)
  - Ссылки и изображения
  - Цитаты и код
- **Режимы редактирования**:
  - Визуальный режим - для удобного написания
  - Markdown режим - для прямого редактирования разметки
- **Поддержка Markdown** - пишите в привычном формате

### 2. Drag & Drop Загрузка Изображений

- **Перетаскивание файлов** прямо в зону загрузки
- **Автоматическая загрузка в Supabase Storage**
- **Индикатор прогресса** загрузки
- **Предпросмотр изображения** сразу после загрузки
- **Валидация**:
  - Только изображения (PNG, JPG, GIF)
  - Максимальный размер 5MB
- **Альтернатива** - выбор из медиатеки

### 3. Предпросмотр Новости

- **Кнопка "Предпросмотр"** в форме создания/редактирования
- **Полноценный рендеринг** новости как на сайте:
  - Форматированный текст (Markdown → HTML)
  - Изображения
  - Заголовки и списки
  - Цитаты и код
- **Режим чтения** - посмотрите как будет выглядеть новость для пользователей

---

## 🚀 Как Использовать

### Доступ к Системе

1. Перейдите на https://kfa-website.vercel.app/dashboard/news
2. Войдите как администратор (требуется право `content.view`)

### Создание Новости

1. **Нажмите кнопку "Добавить новость"**

2. **Заполните форму:**
   - **Заголовок** (обязательно) - название новости
   - **URL (slug)** - автоматически генерируется из заголовка
   - **Краткое описание** - отображается в списке новостей
   - **Контент** (обязательно) - используйте Rich Text Editor:

     ```markdown
     # Основной заголовок

     Введение к новости...

     ## Подзаголовок

     - Пункт списка 1
     - Пункт списка 2

     **Жирный текст** и _курсив_

     [Ссылка](https://example.com)
     ```

3. **Загрузите изображение:**
   - **Drag & Drop:** Перетащите файл в зону загрузки
   - **Клик:** Нажмите на зону загрузки для выбора файла
   - **Медиатека:** Нажмите "Или выбрать из медиатеки"

4. **Настройте параметры:**
   - **Статус:**
     - `Черновик` - не видна пользователям
     - `Опубликовано` - доступна всем
     - `Архив` - скрыта, но сохранена
   - **Избранное** - отметьте для показа в топе

5. **Предпросмотр:**
   - Нажмите кнопку "Предпросмотр" для просмотра
   - Проверьте форматирование и внешний вид

6. **Сохраните:**
   - Нажмите "Создать новость"
   - Новость будет сохранена в Supabase

### Редактирование Новости

1. Найдите новость в списке (используйте поиск/фильтры)
2. Нажмите кнопку "Редактировать"
3. Измените нужные поля
4. Используйте "Предпросмотр" для проверки
5. Нажмите "Сохранить изменения"

### Удаление Новости

1. Найдите новость в списке
2. Нажмите кнопку "Удалить"
3. Подтвердите действие

---

## 🎨 Возможности Rich Text Editor

### Панель Инструментов

| Кнопка | Действие     | Markdown       |
| ------ | ------------ | -------------- |
| H1     | Заголовок 1  | `# Текст`      |
| H2     | Заголовок 2  | `## Текст`     |
| H3     | Заголовок 3  | `### Текст`    |
| **B**  | Жирный       | `**текст**`    |
| _I_    | Курсив       | `_текст_`      |
| •      | Список       | `- пункт`      |
| 1.     | Нумерованный | `1. пункт`     |
| 🔗     | Ссылка       | `[текст](url)` |
| 🖼️     | Изображение  | `![alt](url)`  |
| "      | Цитата       | `> текст`      |
| <>     | Код          | `` `код` ``    |

### Горячие Клавиши

- `Ctrl+B` - Жирный (в некоторых браузерах)
- `Ctrl+I` - Курсив (в некоторых браузерах)
- Выделите текст и нажмите кнопку на панели

---

## 📤 Загрузка Изображений

### Способы Загрузки

#### 1. Drag & Drop (Рекомендуется)

```
┌─────────────────────────────────┐
│   📁 Перетащите изображение    │
│                                  │
│   Или нажмите для выбора файла  │
└─────────────────────────────────┘
```

- Просто перетащите файл из проводника
- Автоматическая загрузка начнется сразу

#### 2. Клик для Выбора

- Нажмите на зону загрузки
- Выберите файл в диалоге
- Загрузка начнется автоматически

#### 3. Из Медиатеки

- Нажмите "Или выбрать из медиатеки"
- Выберите ранее загруженное изображение

### Требования к Изображениям

- **Форматы:** PNG, JPG, JPEG, GIF
- **Размер:** до 5MB
- **Рекомендуется:** 1200x630px для лучшего отображения

### Что Происходит при Загрузке

1. **Валидация** - проверка типа и размера
2. **Загрузка** в Supabase Storage (`media` bucket)
3. **Создание записи** в таблице `media`
4. **Получение URL** - публичная ссылка на изображение
5. **Автозаполнение** полей `image` и `featured_image_id`

---

## 👁️ Предпросмотр Новости

### Когда Использовать

- ✅ Перед публикацией новой новости
- ✅ После редактирования контента
- ✅ Для проверки форматирования Markdown
- ✅ Для проверки отображения изображений

### Что Показывается

Предпросмотр полностью имитирует отображение новости на сайте:

```
┌────────────────────────────────────────┐
│  ⭐ Избранное (если отмечено)          │
│                                         │
│  Заголовок Новости                     │
│  ────────────────────                  │
│                                         │
│  Краткое описание (excerpt)            │
│                                         │
│  [Изображение]                         │
│  ─────────────                         │
│                                         │
│  Форматированный контент с:            │
│  • Заголовками                         │
│  • Списками                            │
│  • Ссылками                            │
│  • Цитатами                            │
│  • Кодом                               │
│                                         │
│  [Закрыть предпросмотр]                │
└────────────────────────────────────────┘
```

---

## 📋 Фильтрация и Поиск

### Поиск по Заголовку

```
🔍 [Введите заголовок...]
```

- Поиск в реальном времени
- Регистронезависимый

### Фильтр по Статусу

```
🎛️ [Все статусы ▼]
   - Все статусы
   - Черновик
   - Опубликовано
   - Архив
```

### Пагинация

- 10 новостей на странице
- Навигация: "Назад" / "Вперед"
- Отображение текущей страницы

---

## 🗄️ Структура Данных

### Таблица `news` в Supabase

```sql
CREATE TABLE news (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  slug TEXT UNIQUE NOT NULL,
  content TEXT NOT NULL,
  excerpt TEXT,
  image TEXT,
  featured_image_id INTEGER REFERENCES media(id),
  status TEXT DEFAULT 'draft', -- 'draft', 'published', 'archived'
  featured BOOLEAN DEFAULT FALSE,
  author_id UUID REFERENCES auth.users(id),
  published_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### Storage Bucket

- **Имя:** `media`
- **Путь изображений:** `news/{filename}`
- **Публичный доступ:** да
- **CDN:** автоматически через Supabase

---

## 🔐 Права Доступа

### Требуемые Роли

Для доступа к `/dashboard/news` требуется одна из ролей:

- `admin` - полный доступ
- `editor` - может создавать/редактировать/удалять
- `moderator` - может редактировать

### Требуемые Разрешения

- `content.view` - просмотр панели управления новостями
- `content.create` - создание новостей
- `content.update` - редактирование новостей
- `content.delete` - удаление новостей

---

## 🐛 Устранение Неполадок

### Изображение не загружается

**Проблема:** Ошибка при загрузке изображения

**Решение:**

1. Проверьте размер файла (max 5MB)
2. Убедитесь что файл - изображение (PNG/JPG/GIF)
3. Проверьте подключение к Supabase
4. Проверьте права на bucket `media` в Supabase

### Новость не сохраняется

**Проблема:** Ошибка при сохранении

**Решение:**

1. Проверьте обязательные поля (заголовок, контент)
2. Проверьте что slug уникален
3. Проверьте подключение к Supabase
4. Посмотрите ошибки в консоли браузера (F12)

### Markdown не отображается

**Проблема:** Текст показывается без форматирования

**Решение:**

1. Используйте предпросмотр для проверки
2. Проверьте синтаксис Markdown
3. Убедитесь что контент сохранен

---

## 📊 API Endpoints (Supabase)

Все операции идут напрямую в Supabase через `supabase-news.ts`:

```typescript
// Получить все новости
supabaseNewsAPI.getAll({ page: 1, status: 'published' });

// Получить одну новость
supabaseNewsAPI.getById(123);

// Создать новость
supabaseNewsAPI.create({ title: '...', content: '...' });

// Обновить новость
supabaseNewsAPI.update(123, { title: 'New Title' });

// Удалить новость
supabaseNewsAPI.delete(123);

// Опубликовать
supabaseNewsAPI.publish(123);

// Сделать избранной
supabaseNewsAPI.toggleFeatured(123);
```

---

## 🎯 Best Practices

### Написание Новостей

1. **Заголовок:**
   - Краткий и понятный
   - Отражает суть новости
   - 50-70 символов оптимально

2. **Excerpt (Краткое описание):**
   - 150-200 символов
   - Резюме основной идеи
   - Без форматирования

3. **Контент:**
   - Используйте заголовки для структуры
   - Разбивайте на абзацы
   - Добавляйте списки для удобства чтения
   - Используйте изображения

4. **Изображение:**
   - Обязательно для featured новостей
   - Высокое качество
   - Релевантное содержанию

### SEO Оптимизация

- ✅ Уникальный slug для каждой новости
- ✅ Содержательный excerpt
- ✅ Качественные изображения с alt-текстом
- ✅ Правильная структура заголовков (H1 → H2 → H3)

### Workflow

1. **Черновик** → пишите и редактируйте
2. **Предпросмотр** → проверьте внешний вид
3. **Опубликовано** → станет доступна пользователям
4. **Избранное** → для важных новостей

---

## 📦 Созданные Компоненты

### 1. `RichTextEditor.tsx`

```typescript
<RichTextEditor
  value={content}
  onChange={setContent}
  placeholder="Введите текст..."
  error={errors.content}
/>
```

**Функции:**

- Панель форматирования
- Визуальный/Markdown режимы
- Вставка ссылок и изображений
- Валидация

### 2. `ImageUploadZone.tsx`

```typescript
<ImageUploadZone
  value={imageUrl}
  onChange={(url, mediaId) => {
    setImageUrl(url);
    setMediaId(mediaId);
  }}
  onError={handleError}
/>
```

**Функции:**

- Drag & Drop
- Прогресс загрузки
- Предпросмотр
- Валидация

### 3. `NewsPreview.tsx`

```typescript
<NewsPreview
  title={title}
  excerpt={excerpt}
  content={content}
  image={image}
  featured={featured}
  isOpen={showPreview}
  onClose={() => setShowPreview(false)}
/>
```

**Функции:**

- Полный рендеринг Markdown
- Стилизация
- Responsive дизайн

---

## 🎉 Готово к Использованию!

Система управления новостями полностью функциональна и готова к использованию.

### Следующие Шаги:

1. ✅ Зайдите на https://kfa-website.vercel.app/dashboard/news
2. ✅ Создайте первую новость
3. ✅ Протестируйте все функции
4. ✅ Опубликуйте новость
5. ✅ Проверьте отображение на сайте

---

## 📞 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте это руководство
2. Посмотрите логи в консоли браузера (F12)
3. Проверьте Supabase Dashboard
4. Обратитесь к разработчику

---

**Создано:** 2025-11-13
**Версия:** 1.0.0
**Статус:** Production Ready ✅
