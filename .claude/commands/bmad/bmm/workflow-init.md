# workflow-init

Initialize BMM workflow for the project.

This command runs the workflow-init workflow from BMM module to set up project tracking.

---

Load agent: `{project-root}/bmad/bmm/agents/analyst.agent.yaml`

Then execute workflow: `{project-root}/bmad/bmm/workflows/workflow-status/init/workflow.yaml`
