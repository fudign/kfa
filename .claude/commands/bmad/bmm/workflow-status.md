# workflow-status

Check BMM workflow status and get next action recommendation.

This command displays current workflow phase, progress, and suggests next steps.

---

Load agent: `{project-root}/bmad/bmm/agents/analyst.agent.yaml`

Then execute workflow: `{project-root}/bmad/bmm/workflows/workflow-status/workflow.yaml`
