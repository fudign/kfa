# 🚀 БЫСТРЫЙ СТАРТ - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ✅ ЧТО УЖЕ ГОТОВО

Вся разработка завершена! Осталось только запустить.

**Проверено:**

- ✅ Node.js установлен (v23.6.0)
- ✅ Все файлы созданы (22 файла)
- ✅ Документация готова
- ✅ Скрипты подготовлены

---

## 📋 ШАГ 1: ЗАПУСК УСТАНОВКИ

### Вариант A: PowerShell (РЕКОМЕНДУЕТСЯ для Windows)

1. Откройте **PowerShell** (правой кнопкой → "Запуск от имени администратора")

2. Перейдите в папку:

```powershell
cd E:\CODE\kfa\BMAD-METHOD\kfa-backend\kfa-api
```

3. Запустите установку:

```powershell
.\setup-news-system.ps1
```

Если появится ошибка политики выполнения:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
.\setup-news-system.ps1
```

### Вариант B: CMD (.bat файл)

1. Откройте **Командную строку** (cmd)

2. Перейдите в папку:

```cmd
cd E:\CODE\kfa\BMAD-METHOD\kfa-backend\kfa-api
```

3. Запустите:

```cmd
setup-news-system.bat
```

### Вариант C: Вручную (если скрипты не работают)

```bash
cd E:\CODE\kfa\BMAD-METHOD\kfa-backend\kfa-api

# 1. Миграции
php artisan migrate

# 2. Права (через tinker)
php artisan tinker
```

В tinker выполните:

```php
$admin = \Spatie\Permission\Models\Role::where('name', 'admin')->first();
$admin->givePermissionTo(['content.view', 'content.create', 'content.update', 'content.delete', 'media.view', 'media.upload', 'media.delete']);
exit
```

---

## 📋 ШАГ 2: ЗАПУСК СЕРВЕРОВ

### Терминал 1: Backend

```bash
cd E:\CODE\kfa\BMAD-METHOD\kfa-backend\kfa-api
php artisan serve
```

**Ожидаемый вывод:**

```
Starting Laravel development server: http://127.0.0.1:8000
```

### Терминал 2: Frontend

Откройте **новый терминал**:

```bash
cd E:\CODE\kfa\BMAD-METHOD\kfa-website
npm run dev
```

**Ожидаемый вывод:**

```
VITE ready in XXX ms
Local:   http://localhost:5173/
```

---

## 📋 ШАГ 3: ОТКРЫТИЕ В БРАУЗЕРЕ

1. Откройте браузер
2. Перейдите: **http://localhost:5173**
3. Войдите как администратор
4. В боковом меню → **"Новости"** 📰

---

## 🧪 ШАГ 4: БЫСТРОЕ ТЕСТИРОВАНИЕ

### A. Тест Backend API

Откройте **новый терминал** и выполните:

```bash
# Проверка доступности API
curl http://localhost:8000/api/news
```

**Ожидаемый результат:** JSON с пустым массивом новостей

### B. Тест Frontend UI

1. В браузере откройте: http://localhost:5173/dashboard/news
2. Нажмите **"Добавить новость"**
3. Заполните форму:
   - Заголовок: "Тестовая новость"
   - Содержимое: "Это тестовая новость для проверки системы"
   - Статус: Draft
4. Нажмите **"Сохранить"**

**Ожидаемый результат:** Новость появится в списке

### C. Тест загрузки изображения

1. Откройте созданную новость
2. Нажмите на кнопку выбора изображения
3. Загрузите изображение через MediaPicker
4. Сохраните

**Ожидаемый результат:** Изображение прикрепится к новости

---

## ❗ РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "PHP не найден"

**Решение:**

1. Установите PHP: https://windows.php.net/download/
2. Или через Chocolatey: `choco install php`
3. Перезапустите терминал

### Проблема: "Миграции не выполняются"

**Решение:**

```bash
# Проверьте подключение к БД
php artisan migrate:status

# Если нужно, пересоздайте БД
php artisan migrate:fresh --seed
```

### Проблема: "Cannot find module"

**Решение:**

```bash
cd kfa-website
npm install
npm run dev
```

### Проблема: "403 Forbidden" в браузере

**Решение:** Права не назначены. Выполните Шаг 1 снова.

### Проблема: "CORS error"

**Решение:** Проверьте, что Backend запущен на http://localhost:8000

---

## 📊 ПРОВЕРКА УСПЕШНОЙ УСТАНОВКИ

После всех шагов у вас должно быть:

- ✅ Backend работает на http://localhost:8000
- ✅ Frontend работает на http://localhost:5173
- ✅ Миграции выполнены (2 новые таблицы)
- ✅ Права назначены роли admin
- ✅ В меню дашборда появились:
  - 📰 Новости
  - 📅 События
  - ✓ Участники
- ✅ API отвечает на запросы
- ✅ Можно создать новость через UI

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Создайте несколько тестовых новостей
2. ✅ Загрузите изображения
3. ✅ Попробуйте разные статусы
4. ✅ Протестируйте фильтрацию
5. ✅ Проверьте модерацию (approve, publish)

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

**Детальная документация:**

- `START-HERE.md` - Краткая инструкция
- `TESTING-PLAN.md` - Полный план тестирования
- `NEWS-SYSTEM-SETUP.md` - Документация API

**Скрипты:**

- `setup-news-system.ps1` - PowerShell установка
- `setup-news-system.bat` - CMD установка
- `setup-news-system.sh` - Linux/Mac установка

---

## 💡 СОВЕТЫ

1. **Держите оба сервера запущенными** (Backend + Frontend)
2. **Используйте инструменты разработчика** (F12) для отладки
3. **Проверяйте консоль** на наличие ошибок
4. **Создавайте backup БД** перед экспериментами

---

## 🎉 ГОТОВО!

Теперь у вас полностью работающая система управления новостями!

**Вопросы?** См. раздел "Решение проблем" выше.

**Удачи!** 🚀
