# СКРИПТЫ АВТОМАТИЗАЦИИ КОНВЕРТАЦИИ ДОКУМЕНТОВ КФА

Автоматические скрипты для конвертации всех 26 документов КФА из Markdown в форматы PDF и DOCX, а также создания архивов для скачивания.

---

## 📋 СОДЕРЖАНИЕ

1. [Обзор](#обзор)
2. [Требования](#требования)
3. [Установка зависимостей](#установка-зависимостей)
4. [Использование](#использование)
5. [Структура выходных файлов](#структура-выходных-файлов)
6. [Устранение неполадок](#устранение-неполадок)

---

## 🎯 ОБЗОР

Скрипты автоматизируют процесс:
- ✅ Конвертации 26 документов из Markdown в PDF и DOCX
- ✅ Создания 9 архивов для скачивания (полные и по категориям)
- ✅ Генерации статистики и логов
- ✅ Проверки всех зависимостей

**Результат:** Готовые файлы для публикации на веб-сайте КФА

---

## 📦 ТРЕБОВАНИЯ

### Обязательные зависимости

#### 1. **Pandoc** (конвертер документов)
- **Версия:** 2.0 или выше
- **Назначение:** Конвертация Markdown → PDF, DOCX

#### 2. **LaTeX** (для создания PDF)
- **Рекомендуемый:** TeX Live или MiKTeX
- **Назначение:** Движок для создания PDF файлов

#### 3. **ZIP-утилита** (для создания архивов)
- **Linux/Mac:** `zip` (обычно предустановлен)
- **Windows:** `7-Zip` или встроенный `Compress-Archive` (PowerShell)

---

## 🔧 УСТАНОВКА ЗАВИСИМОСТЕЙ

### Linux (Ubuntu/Debian)

```bash
# Установка Pandoc
sudo apt-get update
sudo apt-get install pandoc

# Установка LaTeX (TeX Live)
sudo apt-get install texlive-xetex texlive-lang-cyrillic

# Установка zip (обычно уже установлен)
sudo apt-get install zip

# Проверка установки
pandoc --version
xelatex --version
zip --version
```

### macOS

```bash
# Установка Homebrew (если не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка Pandoc
brew install pandoc

# Установка LaTeX (MacTeX - базовая версия)
brew install --cask basictex

# После установки LaTeX, обновите PATH:
echo 'export PATH=/Library/TeX/texbin:$PATH' >> ~/.zshrc
source ~/.zshrc

# Установка дополнительных пакетов LaTeX
sudo tlmgr update --self
sudo tlmgr install collection-langcyrillic

# zip обычно предустановлен
```

### Windows

#### Способ 1: С помощью Chocolatey (рекомендуется)

```powershell
# Установка Chocolatey (если не установлен)
# Запустите PowerShell от имени администратора:
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установка Pandoc
choco install pandoc -y

# Установка MiKTeX (LaTeX)
choco install miktex -y

# Установка 7-Zip
choco install 7zip -y

# Перезапустите PowerShell после установки
```

#### Способ 2: Ручная установка

1. **Pandoc:**
   - Скачайте: https://pandoc.org/installing.html
   - Установите MSI-файл для Windows

2. **MiKTeX (LaTeX):**
   - Скачайте: https://miktex.org/download
   - Установите базовую версию
   - При первом запуске разрешите автоматическую установку пакетов

3. **7-Zip:**
   - Скачайте: https://www.7-zip.org/
   - Установите в стандартную папку `C:\Program Files\7-Zip\`

4. **Добавьте в PATH:**
   - Откройте "Панель управления" → "Система" → "Дополнительные параметры системы"
   - "Переменные среды" → "Path" → "Изменить"
   - Добавьте пути:
     - `C:\Program Files\Pandoc\`
     - `C:\Program Files\MiKTeX\miktex\bin\x64\`
     - `C:\Program Files\7-Zip\`

5. **Проверка установки:**
   ```powershell
   pandoc --version
   xelatex --version
   "C:\Program Files\7-Zip\7z.exe" --help
   ```

---

## 🚀 ИСПОЛЬЗОВАНИЕ

### Linux / macOS

1. **Перейдите в директорию со скриптами:**
   ```bash
   cd E:/CODE/kfa/BMAD-METHOD/docs/kfa/scripts
   ```

2. **Сделайте скрипт исполняемым:**
   ```bash
   chmod +x convert-documents.sh
   ```

3. **Запустите конвертацию:**
   ```bash
   ./convert-documents.sh
   ```

4. **Дождитесь завершения:**
   - Скрипт выведет прогресс каждого документа
   - В конце отобразится статистика
   - Время выполнения: ~5-10 минут (зависит от мощности системы)

### Windows (PowerShell)

1. **Откройте PowerShell от имени администратора:**
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (Администратор)"

2. **Разрешите выполнение скриптов (первый раз):**
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
   ```

3. **Перейдите в директорию со скриптами:**
   ```powershell
   cd E:\CODE\kfa\BMAD-METHOD\docs\kfa\scripts
   ```

4. **Запустите конвертацию:**
   ```powershell
   .\convert-documents.ps1
   ```

5. **Дождитесь завершения:**
   - Скрипт выведет прогресс с цветными статусами
   - В конце отобразится статистика
   - Нажмите любую клавишу для выхода

---

## 📂 СТРУКТУРА ВЫХОДНЫХ ФАЙЛОВ

После выполнения скрипта создается следующая структура:

```
docs/kfa/output/
│
├── pdf/                                # PDF файлы
│   ├── charter/                        # Учредительные документы (8 PDF)
│   │   ├── Устав-КФА-2025-10-23.pdf
│   │   ├── Положение-о-членстве-КФА.pdf
│   │   └── ...
│   │
│   ├── regulations/                    # Регулирующие положения (4 PDF)
│   │   ├── Положение-о-сертификации-специалистов-КФА.pdf
│   │   └── ...
│   │
│   ├── web/                            # Веб-документы (2 PDF)
│   │   ├── Политика-конфиденциальности-сайта-КФА.pdf
│   │   └── Пользовательское-соглашение-сайта-КФА.pdf
│   │
│   └── forms/                          # Операционные формы (4 PDF)
│       ├── Соглашение-о-конфиденциальности-NDA.pdf
│       └── ...
│
├── docx/                               # DOCX файлы (аналогичная структура)
│   ├── charter/
│   ├── regulations/
│   ├── web/
│   └── forms/
│
├── archives/                           # ZIP-архивы для скачивания
│   ├── KFA-Documents-Full-PDF.zip      # ~50 MB - все документы (PDF)
│   ├── KFA-Documents-Full-DOCX.zip     # ~30 MB - все документы (DOCX)
│   ├── KFA-Charter-Documents-PDF.zip   # ~25 MB - учредительные (PDF)
│   ├── KFA-Charter-Documents-DOCX.zip  # ~15 MB - учредительные (DOCX)
│   ├── KFA-Regulations-PDF.zip         # ~15 MB - положения (PDF)
│   ├── KFA-Regulations-DOCX.zip        # ~10 MB - положения (DOCX)
│   ├── KFA-Forms-PDF.zip               # ~5 MB - формы (PDF)
│   ├── KFA-Forms-DOCX.zip              # ~3 MB - формы (DOCX)
│   └── KFA-Membership-Forms-DOCX.zip   # ~1 MB - формы для членства
│
└── conversion.log                      # Лог конвертации
```

---

## 📊 СТАТИСТИКА

### Ожидаемые результаты:

| Категория | PDF | DOCX | Архивов |
|-----------|-----|------|---------|
| Учредительные документы | 8 | 8 | 2 |
| Регулирующие положения | 4 | 4 | 2 |
| Веб-документы | 2 | 2 | 0 |
| Операционные формы | 4 | 4 | 3 |
| **ИТОГО** | **18** | **18** | **9** |

### Размеры архивов:

- **Полные архивы:** ~80 MB (PDF + DOCX)
- **По категориям:** ~70 MB (все категории)
- **Общий объем:** ~150 MB

---

## 🐛 УСТРАНЕНИЕ НЕПОЛАДОК

### Проблема 1: "pandoc: command not found"

**Причина:** Pandoc не установлен или не добавлен в PATH

**Решение:**
```bash
# Linux/Mac
which pandoc
sudo apt-get install pandoc  # или brew install pandoc

# Windows
where pandoc
# Переустановите Pandoc и добавьте в PATH
```

---

### Проблема 2: "xelatex: command not found"

**Причина:** LaTeX не установлен

**Решение:**
```bash
# Linux
sudo apt-get install texlive-xetex texlive-lang-cyrillic

# Mac
brew install --cask basictex
sudo tlmgr install collection-langcyrillic

# Windows
# Установите MiKTeX с https://miktex.org/
```

---

### Проблема 3: Ошибки кириллицы в PDF

**Причина:** Отсутствуют русские шрифты для LaTeX

**Решение:**
```bash
# Linux
sudo apt-get install texlive-lang-cyrillic fonts-liberation

# Mac
sudo tlmgr install collection-langcyrillic

# Windows
# В MiKTeX Console: Packages → Install → cyrillic
```

---

### Проблема 4: "Permission denied" (Linux/Mac)

**Причина:** Недостаточно прав для выполнения скрипта

**Решение:**
```bash
chmod +x convert-documents.sh
./convert-documents.sh

# Если ошибка остается:
sudo ./convert-documents.sh
```

---

### Проблема 5: Скрипт PowerShell не запускается

**Причина:** Политика выполнения скриптов

**Решение:**
```powershell
# Проверьте текущую политику
Get-ExecutionPolicy

# Разрешите выполнение
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Или запустите один раз:
PowerShell -ExecutionPolicy Bypass -File .\convert-documents.ps1
```

---

### Проблема 6: 7-Zip не найден (Windows)

**Причина:** 7-Zip не установлен или установлен в нестандартную папку

**Решение:**
1. Установите 7-Zip: https://www.7-zip.org/
2. Или отредактируйте скрипт, указав правильный путь к `7z.exe`
3. Или используйте встроенный `Compress-Archive` (медленнее, но работает)

---

### Проблема 7: Медленная конвертация

**Причина:** MiKTeX загружает пакеты при первом запуске

**Решение:**
- При первом запуске MiKTeX спросит о разрешении загружать пакеты
- Выберите "Yes" или "Always" для автоматической установки
- Последующие запуски будут быстрее

---

### Проблема 8: Ошибки в логе

**Причина:** Проблемы с отдельными документами

**Решение:**
```bash
# Проверьте лог
cat output/conversion.log

# Или в Windows
notepad output\conversion.log

# Найдите конкретные ошибки и конвертируйте вручную:
pandoc "Устав-КФА-2025-10-23.md" -o "Устав-КФА.pdf" --pdf-engine=xelatex
```

---

## 🔍 ПРОВЕРКА РЕЗУЛЬТАТОВ

### Чек-лист после конвертации:

- [ ] Все 18 PDF файлов созданы
- [ ] Все 18 DOCX файлов созданы
- [ ] Все 9 архивов созданы
- [ ] Кириллица отображается корректно
- [ ] Оглавления в документах работают
- [ ] Размеры файлов разумные (не пустые)
- [ ] Лог не содержит критических ошибок

### Ручная проверка:

```bash
# Подсчет файлов
find output/pdf -name "*.pdf" | wc -l    # Должно быть 18
find output/docx -name "*.docx" | wc -l  # Должно быть 18
find output/archives -name "*.zip" | wc -l  # Должно быть 9

# Проверка размеров архивов
ls -lh output/archives/

# Тест открытия PDF
xdg-open output/pdf/charter/Устав-КФА-2025-10-23.pdf  # Linux
open output/pdf/charter/Устав-КФА-2025-10-23.pdf      # Mac
start output\pdf\charter\Устав-КФА-2025-10-23.pdf     # Windows
```

---

## 📤 ЗАГРУЗКА НА ВЕБ-САЙТ

После успешной конвертации:

1. **Скопируйте файлы на веб-сервер:**
   ```bash
   # Пример для Linux-сервера
   scp -r output/pdf user@kfa.kg:/var/www/html/documents/
   scp -r output/archives user@kfa.kg:/var/www/html/download/
   ```

2. **Или загрузите через FTP:**
   - Используйте FileZilla или WinSCP
   - Загрузите `output/pdf/` → `/var/www/html/documents/`
   - Загрузите `output/archives/` → `/var/www/html/download/`

3. **Проверьте доступность:**
   - https://www.kfa.kg/documents/charter/Устав-КФА-2025-10-23.pdf
   - https://www.kfa.kg/download/KFA-Documents-Full-PDF.zip

---

## 📞 ПОДДЕРЖКА

**Вопросы по скриптам:**
📧 support@kfa.kg

**Технические проблемы:**
📧 tech@kfa.kg

**Документация Pandoc:**
🌐 https://pandoc.org/MANUAL.html

---

**Powered by:** BMAD Method v6.0
**Версия скриптов:** 1.0
**Последнее обновление:** 23 октября 2025 г.

---

🚀 _"Автоматизация — путь к эффективности!"_
