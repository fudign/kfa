# РУКОВОДСТВО ПО РАЗВЕРТЫВАНИЮ ВЕБ-САЙТА КФА

**Пошаговая инструкция по развертыванию веб-сайта Кыргызского Финансового Альянса**

---

## 📋 СОДЕРЖАНИЕ

1. [Обзор проекта](#обзор-проекта)
2. [Требования к серверу](#требования-к-серверу)
3. [Подготовка документов](#подготовка-документов)
4. [Настройка веб-сервера](#настройка-веб-сервера)
5. [Развертывание документов](#развертывание-документов)
6. [Настройка системы авторизации](#настройка-системы-авторизации)
7. [Интеграция веб-документов](#интеграция-веб-документов)
8. [Тестирование](#тестирование)
9. [Запуск в продакшн](#запуск-в-продакшн)

---

## 🎯 ОБЗОР ПРОЕКТА

### Что развертываем:

**26 документов КФА:**

- 8 учредительных документов
- 6 регулирующих положений
- 2 веб-документа (Privacy Policy, Terms of Use)
- 10 операционных форм

**Форматы:**

- HTML (для просмотра онлайн)
- PDF (для скачивания и печати)
- DOCX (для редактирования форм)

**Уровни доступа:**

- 🌐 Публичный доступ (учредительные документы, формы)
- 🔒 Для членов КФА (регламенты, договоры)
- 🔐 Внутренний (протоколы, финансы)

---

## 💻 ТРЕБОВАНИЯ К СЕРВЕРУ

### Минимальные технические требования:

**Сервер:**

- Ubuntu 20.04 LTS или выше (или CentOS 7+)
- 2 CPU cores
- 4 GB RAM
- 50 GB SSD

**Веб-сервер:**

- Nginx 1.18+ (рекомендуется) или Apache 2.4+
- SSL-сертификат (Let's Encrypt)

**База данных (для авторизации):**

- PostgreSQL 12+ или MySQL 8.0+

**Backend (опционально):**

- Node.js 16+ (для онлайн-форм)
- PHP 8.0+ (альтернатива)

**Дополнительно:**

- Git (для версионирования)
- Certbot (для SSL)
- UFW/iptables (firewall)

---

## 📦 ПОДГОТОВКА ДОКУМЕНТОВ

### Шаг 1: Конвертация Markdown → PDF, DOCX

Используйте скрипты автоматизации:

```bash
# Linux/Mac
cd docs/kfa/scripts
chmod +x convert-documents.sh
./convert-documents.sh

# Windows PowerShell
cd docs\kfa\scripts
.\convert-documents.ps1
```

**Результат:** Папка `docs/kfa/output/` с PDF, DOCX и архивами

---

### Шаг 2: Конвертация Markdown → HTML

```bash
# Установка конвертера
npm install -g marked

# Или используйте pandoc
cd docs/kfa

# Конвертация всех документов в HTML
for file in *.md; do
    pandoc "$file" -o "output/html/${file%.md}.html" \
        --standalone \
        --template=template.html \
        --toc \
        --toc-depth=3 \
        -V lang=ru \
        -c /assets/css/document-style.css
done
```

**Шаблон HTML** (`template.html`):

```html
<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="$description$" />
    <title>$title$ | Кыргызский Финансовый Альянс</title>
    <link rel="stylesheet" href="/assets/css/document-style.css" />
    <link rel="icon" href="/assets/images/favicon.ico" />
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <nav class="navbar">
          <a href="/" class="logo">
            <img src="/assets/images/kfa-logo.png" alt="КФА" />
          </a>
          <ul class="nav-menu">
            <li><a href="/">Главная</a></li>
            <li><a href="/documents/">Документы</a></li>
            <li><a href="/members/">Членство</a></li>
            <li><a href="/education/">Образование</a></li>
            <li><a href="/contacts/">Контакты</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <main class="document-page">
      <div class="container">
        <nav class="breadcrumb">$breadcrumb$</nav>

        <div class="document-wrapper">
          <aside class="document-sidebar">
            <div class="document-actions">
              <a href="$pdf-link$" class="btn btn-primary"> 📥 Скачать PDF </a>
              <button onclick="window.print()" class="btn btn-secondary">🖨️ Печать</button>
            </div>

            <nav class="toc">
              <h3>Оглавление</h3>
              $toc$
            </nav>
          </aside>

          <article class="document-content">$body$</article>
        </div>
      </div>
    </main>

    <footer class="site-footer">
      <div class="container">
        <div class="footer-links">
          <a href="/privacy-policy">Политика конфиденциальности</a> | <a href="/terms-of-use">Пользовательское соглашение</a> |
          <a href="/contacts">Контакты</a>
        </div>
        <p>&copy; 2025 Кыргызский Финансовый Альянс. Все права защищены.</p>
      </div>
    </footer>

    <script src="/assets/js/document.js"></script>
  </body>
</html>
```

---

## 🌐 НАСТРОЙКА ВЕБ-СЕРВЕРА

### Вариант 1: Nginx (рекомендуется)

#### Шаг 1: Установка Nginx

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install nginx -y

# Проверка
sudo systemctl status nginx
```

#### Шаг 2: Конфигурация Nginx

Создайте файл `/etc/nginx/sites-available/kfa.kg`:

```nginx
# HTTP → HTTPS редирект
server {
    listen 80;
    listen [::]:80;
    server_name kfa.kg www.kfa.kg;

    return 301 https://$server_name$request_uri;
}

# HTTPS основной сервер
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name kfa.kg www.kfa.kg;

    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/kfa.kg/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kfa.kg/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Корневая директория
    root /var/www/kfa.kg/public;
    index index.html index.php;

    # Логи
    access_log /var/log/nginx/kfa.kg-access.log;
    error_log /var/log/nginx/kfa.kg-error.log;

    # Публичные документы
    location /documents/ {
        alias /var/www/kfa.kg/documents/;
        autoindex off;
        try_files $uri $uri/ =404;
    }

    # Файлы для скачивания
    location /download/ {
        alias /var/www/kfa.kg/download/;
        autoindex off;
        add_header Content-Disposition "attachment";
    }

    # Формы (публичный доступ)
    location /forms/ {
        alias /var/www/kfa.kg/forms/;
        autoindex off;
    }

    # Зона для членов (требует авторизации)
    location /members/ {
        alias /var/www/kfa.kg/members/;
        auth_basic "Только для членов КФА";
        auth_basic_user_file /etc/nginx/.htpasswd_kfa;
        autoindex off;
    }

    # Веб-документы
    location /privacy-policy {
        try_files /web/privacy-policy.html =404;
    }

    location /terms-of-use {
        try_files /web/terms-of-use.html =404;
    }

    # Статические ресурсы с кешированием
    location /assets/ {
        alias /var/www/kfa.kg/assets/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # PHP обработка (если используется)
    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/var/run/php/php8.0-fpm.sock;
    }

    # Защита от прямого доступа к .md файлам
    location ~ \.md$ {
        deny all;
    }

    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

#### Шаг 3: Активация конфигурации

```bash
# Создать символическую ссылку
sudo ln -s /etc/nginx/sites-available/kfa.kg /etc/nginx/sites-enabled/

# Проверить конфигурацию
sudo nginx -t

# Перезагрузить Nginx
sudo systemctl reload nginx
```

---

### Вариант 2: Apache (альтернатива)

#### Шаг 1: Установка Apache

```bash
sudo apt-get install apache2 -y
sudo a2enmod ssl rewrite headers
```

#### Шаг 2: Конфигурация Apache

Создайте файл `/etc/apache2/sites-available/kfa.kg.conf`:

```apache
<VirtualHost *:80>
    ServerName kfa.kg
    ServerAlias www.kfa.kg
    Redirect permanent / https://kfa.kg/
</VirtualHost>

<VirtualHost *:443>
    ServerName kfa.kg
    ServerAlias www.kfa.kg

    DocumentRoot /var/www/kfa.kg/public

    # SSL
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/kfa.kg/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/kfa.kg/privkey.pem

    # Публичные документы
    Alias /documents /var/www/kfa.kg/documents
    <Directory /var/www/kfa.kg/documents>
        Require all granted
        Options -Indexes
    </Directory>

    # Зона для членов
    Alias /members /var/www/kfa.kg/members
    <Directory /var/www/kfa.kg/members>
        AuthType Basic
        AuthName "Только для членов КФА"
        AuthUserFile /etc/apache2/.htpasswd_kfa
        Require valid-user
    </Directory>

    # Логи
    ErrorLog ${APACHE_LOG_DIR}/kfa.kg-error.log
    CustomLog ${APACHE_LOG_DIR}/kfa.kg-access.log combined
</VirtualHost>
```

#### Шаг 3: Активация сайта

```bash
sudo a2ensite kfa.kg
sudo apache2ctl configtest
sudo systemctl reload apache2
```

---

## 🚀 РАЗВЕРТЫВАНИЕ ДОКУМЕНТОВ

### Шаг 1: Создание структуры директорий

```bash
# Создать основные директории
sudo mkdir -p /var/www/kfa.kg/{public,documents,download,forms,members,web,assets}

# Создать поддиректории
sudo mkdir -p /var/www/kfa.kg/documents/{charter,regulations}
sudo mkdir -p /var/www/kfa.kg/forms/{membership,education,events,certification}
sudo mkdir -p /var/www/kfa.kg/members/{documents,forms}
sudo mkdir -p /var/www/kfa.kg/assets/{css,js,images}

# Установить владельца
sudo chown -R www-data:www-data /var/www/kfa.kg

# Установить права
sudo chmod -R 755 /var/www/kfa.kg
```

---

### Шаг 2: Загрузка файлов на сервер

**Метод 1: SCP (через SSH)**

```bash
# С локального компьютера
scp -r docs/kfa/output/pdf/* user@kfa.kg:/tmp/kfa-upload/pdf/
scp -r docs/kfa/output/archives/* user@kfa.kg:/tmp/kfa-upload/archives/

# На сервере переместить файлы
sudo mv /tmp/kfa-upload/pdf/charter/* /var/www/kfa.kg/documents/charter/
sudo mv /tmp/kfa-upload/pdf/regulations/* /var/www/kfa.kg/documents/regulations/
sudo mv /tmp/kfa-upload/archives/* /var/www/kfa.kg/download/
```

**Метод 2: rsync (синхронизация)**

```bash
# Синхронизация всех файлов
rsync -avz --progress docs/kfa/output/ user@kfa.kg:/var/www/kfa.kg/
```

**Метод 3: Git (рекомендуется для версионирования)**

```bash
# На сервере
cd /var/www/kfa.kg
sudo git init
sudo git remote add origin https://github.com/kfa-kg/documents.git

# С локального компьютера пушим в репозиторий
git add docs/kfa/output/*
git commit -m "Initial document upload"
git push origin main

# На сервере пулим
sudo git pull origin main
```

---

### Шаг 3: Организация файлов по доступу

**Публичные документы:**

```bash
# Учредительные документы (публичный доступ)
/var/www/kfa.kg/documents/charter/
├── Устав-КФА-2025-10-23.pdf
├── Положение-о-членстве-КФА.pdf
├── Базовые-стандарты-профессиональной-деятельности-КФА.pdf
└── Кодекс-профессиональной-этики-КФА.pdf

# Формы (публичный доступ)
/var/www/kfa.kg/forms/
├── membership/
│   ├── application.pdf
│   ├── application.docx
│   └── documents-checklist.pdf
└── certification/
    ├── certification-application.pdf
    └── certification-application.docx
```

**Документы для членов:**

```bash
# Только для членов КФА (требуется авторизация)
/var/www/kfa.kg/members/documents/
├── Регламент-работы-Совета-КФА.pdf
├── Положение-о-финансово-хозяйственной-деятельности-КФА.pdf
└── Дополнительные-положения-КФА.pdf

/var/www/kfa.kg/members/forms/
├── nda-template.pdf
├── nda-template.docx
├── membership-agreement.pdf
└── membership-agreement.docx
```

**Архивы для скачивания:**

```bash
/var/www/kfa.kg/download/
├── KFA-Documents-Full-PDF.zip
├── KFA-Documents-Full-DOCX.zip
├── KFA-Charter-Documents-PDF.zip
├── KFA-Regulations-PDF.zip
└── KFA-Forms-PDF.zip
```

---

## 🔐 НАСТРОЙКА СИСТЕМЫ АВТОРИЗАЦИИ

### Вариант 1: HTTP Basic Auth (простой, для старта)

```bash
# Установить утилиту
sudo apt-get install apache2-utils -y

# Создать файл с паролями
sudo htpasswd -c /etc/nginx/.htpasswd_kfa admin
# Введите пароль

# Добавить других пользователей
sudo htpasswd /etc/nginx/.htpasswd_kfa member1
sudo htpasswd /etc/nginx/.htpasswd_kfa member2

# Установить права
sudo chmod 640 /etc/nginx/.htpasswd_kfa
sudo chown www-data:www-data /etc/nginx/.htpasswd_kfa
```

---

### Вариант 2: Полноценная система авторизации (рекомендуется)

#### База данных пользователей:

**Схема PostgreSQL:**

```sql
-- Создать базу данных
CREATE DATABASE kfa_members;

-- Таблица пользователей
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    full_name VARCHAR(255) NOT NULL,
    organization VARCHAR(255),
    membership_type VARCHAR(50) NOT NULL, -- 'full', 'associate', 'affiliate'
    membership_status VARCHAR(50) NOT NULL, -- 'active', 'suspended', 'expired'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_login TIMESTAMP
);

-- Таблица сессий
CREATE TABLE sessions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    session_token VARCHAR(255) UNIQUE NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица доступа к документам
CREATE TABLE document_access_log (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    document_path VARCHAR(500) NOT NULL,
    accessed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ip_address INET
);

-- Индексы
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_sessions_token ON sessions(session_token);
CREATE INDEX idx_sessions_user ON sessions(user_id);
```

#### Backend API (Node.js + Express):

**Пример `auth-middleware.js`:**

```javascript
const jwt = require('jsonwebtoken');
const pool = require('./db'); // PostgreSQL connection pool

const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key-change-in-production';

const authMiddleware = async (req, res, next) => {
  try {
    // Получить токен из cookie или заголовка
    const token = req.cookies.auth_token || req.headers.authorization?.replace('Bearer ', '');

    if (!token) {
      return res.status(401).json({ error: 'Требуется авторизация' });
    }

    // Проверить токен
    const decoded = jwt.verify(token, JWT_SECRET);

    // Проверить существование пользователя и статус членства
    const result = await pool.query('SELECT id, email, full_name, membership_type, membership_status FROM users WHERE id = $1', [
      decoded.userId,
    ]);

    if (result.rows.length === 0) {
      return res.status(401).json({ error: 'Пользователь не найден' });
    }

    const user = result.rows[0];

    if (user.membership_status !== 'active') {
      return res.status(403).json({ error: 'Членство приостановлено или истекло' });
    }

    // Добавить пользователя в request
    req.user = user;
    next();
  } catch (error) {
    return res.status(401).json({ error: 'Невалидный токен' });
  }
};

module.exports = authMiddleware;
```

**Пример роутов `documents-routes.js`:**

```javascript
const express = require('express');
const path = require('path');
const authMiddleware = require('./auth-middleware');

const router = express.Router();

// Публичные документы (без авторизации)
router.get('/documents/charter/:filename', (req, res) => {
  const filename = req.params.filename;
  const filePath = path.join(__dirname, '../public/documents/charter', filename);
  res.sendFile(filePath);
});

// Документы для членов (требуется авторизация)
router.get('/members/documents/:filename', authMiddleware, (req, res) => {
  const filename = req.params.filename;
  const filePath = path.join(__dirname, '../members/documents', filename);

  // Логировать доступ
  pool.query('INSERT INTO document_access_log (user_id, document_path, ip_address) VALUES ($1, $2, $3)', [req.user.id, filename, req.ip]);

  res.sendFile(filePath);
});

// Скачивание форм для членов
router.get('/members/forms/:filename', authMiddleware, (req, res) => {
  const filename = req.params.filename;
  const filePath = path.join(__dirname, '../members/forms', filename);
  res.download(filePath);
});

module.exports = router;
```

---

## 🌐 ИНТЕГРАЦИЯ ВЕБ-ДОКУМЕНТОВ

### Политика конфиденциальности и Пользовательское соглашение

**Footer на всех страницах:**

```html
<footer class="site-footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-section">
        <h4>Кыргызский Финансовый Альянс</h4>
        <p>Саморегулируемая организация профессиональных участников рынка ценных бумаг</p>
      </div>

      <div class="footer-section">
        <h4>Документы</h4>
        <ul>
          <li><a href="/documents/charter/">Учредительные документы</a></li>
          <li><a href="/documents/standards/">Стандарты и кодексы</a></li>
          <li><a href="/forms/">Формы и заявления</a></li>
        </ul>
      </div>

      <div class="footer-section">
        <h4>Юридическая информация</h4>
        <ul>
          <li><a href="/privacy-policy">Политика конфиденциальности</a></li>
          <li><a href="/terms-of-use">Пользовательское соглашение</a></li>
          <li><a href="/contacts">Контакты</a></li>
        </ul>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; 2025 Кыргызский Финансовый Альянс. Все права защищены.</p>
    </div>
  </div>
</footer>
```

**Форма регистрации с согласием:**

```html
<form id="registration-form" action="/api/register" method="POST">
  <h2>Регистрация на сайте КФА</h2>

  <!-- Поля формы -->
  <div class="form-group">
    <label for="email">Email *</label>
    <input type="email" id="email" name="email" required />
  </div>

  <div class="form-group">
    <label for="full_name">ФИО *</label>
    <input type="text" id="full_name" name="full_name" required />
  </div>

  <div class="form-group">
    <label for="organization">Организация</label>
    <input type="text" id="organization" name="organization" />
  </div>

  <div class="form-group">
    <label for="password">Пароль *</label>
    <input type="password" id="password" name="password" required />
  </div>

  <!-- Согласия (обязательные чекбоксы) -->
  <div class="agreements">
    <div class="checkbox-wrapper">
      <input type="checkbox" id="agree_privacy" name="agree_privacy" required />
      <label for="agree_privacy">
        Я ознакомлен(а) с
        <a href="/privacy-policy" target="_blank">Политикой конфиденциальности</a>
        и согласен(на) на обработку персональных данных *
      </label>
    </div>

    <div class="checkbox-wrapper">
      <input type="checkbox" id="agree_terms" name="agree_terms" required />
      <label for="agree_terms">
        Я принимаю условия
        <a href="/terms-of-use" target="_blank">Пользовательского соглашения</a> *
      </label>
    </div>
  </div>

  <button type="submit" class="btn btn-primary">Зарегистрироваться</button>
</form>
```

**Cookies Banner:**

```html
<div id="cookie-banner" class="cookie-banner" style="display: none;">
  <div class="cookie-content">
    <p>
      Мы используем cookies для улучшения работы сайта и персонализации контента. Продолжая использовать сайт, вы соглашаетесь с нашей
      <a href="/privacy-policy#cookies">Политикой использования cookies</a>.
    </p>
    <div class="cookie-actions">
      <button onclick="acceptAllCookies()" class="btn btn-primary">Принять все</button>
      <button onclick="manageCookies()" class="btn btn-secondary">Настроить</button>
      <button onclick="rejectCookies()" class="btn btn-text">Отклонить</button>
    </div>
  </div>
</div>

<script>
  // Проверить наличие согласия при загрузке страницы
  document.addEventListener('DOMContentLoaded', function () {
    if (!localStorage.getItem('cookies_accepted')) {
      document.getElementById('cookie-banner').style.display = 'block';
    }
  });

  function acceptAllCookies() {
    localStorage.setItem('cookies_accepted', 'true');
    localStorage.setItem('analytics_enabled', 'true');
    document.getElementById('cookie-banner').style.display = 'none';
    // Активировать Google Analytics или Яндекс.Метрика
    enableAnalytics();
  }

  function rejectCookies() {
    localStorage.setItem('cookies_accepted', 'false');
    localStorage.setItem('analytics_enabled', 'false');
    document.getElementById('cookie-banner').style.display = 'none';
  }

  function manageCookies() {
    // Открыть модальное окно настроек cookies
    window.location.href = '/privacy-policy#manage-cookies';
  }

  function enableAnalytics() {
    // Код активации Google Analytics
    // ...
  }
</script>
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Чек-лист тестирования:

#### 1. Доступность документов:

```bash
# Публичные документы (должны открываться)
curl -I https://kfa.kg/documents/charter/Устав-КФА-2025-10-23.pdf
curl -I https://kfa.kg/forms/membership/application.pdf

# Документы для членов (должен требовать авторизацию)
curl -I https://kfa.kg/members/documents/Регламент-работы-Совета-КФА.pdf
# Ожидаемый ответ: 401 Unauthorized

# С авторизацией
curl -I -u member1:password https://kfa.kg/members/documents/Регламент-работы-Совета-КФА.pdf
# Ожидаемый ответ: 200 OK
```

#### 2. Скачивание архивов:

```bash
# Проверить доступность архивов
curl -I https://kfa.kg/download/KFA-Documents-Full-PDF.zip
curl -I https://kfa.kg/download/KFA-Forms-PDF.zip

# Проверить размеры (должны совпадать с локальными)
curl -sI https://kfa.kg/download/KFA-Documents-Full-PDF.zip | grep Content-Length
```

#### 3. SSL и безопасность:

```bash
# Проверить SSL сертификат
openssl s_client -connect kfa.kg:443 -servername kfa.kg

# Проверить редирект HTTP → HTTPS
curl -I http://kfa.kg
# Ожидаемый ответ: 301 Moved Permanently

# Проверить заголовки безопасности
curl -I https://kfa.kg | grep -E '(Strict-Transport-Security|X-Frame-Options|X-Content-Type-Options)'
```

#### 4. Производительность:

```bash
# Проверить время загрузки главной страницы
curl -o /dev/null -s -w "Time: %{time_total}s\n" https://kfa.kg

# Проверить сжатие (gzip)
curl -H "Accept-Encoding: gzip" -I https://kfa.kg | grep Content-Encoding
# Ожидаемый ответ: Content-Encoding: gzip
```

#### 5. Веб-документы:

- [ ] Политика конфиденциальности открывается: https://kfa.kg/privacy-policy
- [ ] Пользовательское соглашение открывается: https://kfa.kg/terms-of-use
- [ ] Ссылки в Footer работают
- [ ] Cookies Banner отображается для новых пользователей
- [ ] Чекбоксы согласия обязательны в форме регистрации

#### 6. Кросс-браузерное тестирование:

- [ ] Chrome/Edge (Chromium)
- [ ] Firefox
- [ ] Safari
- [ ] Мобильные браузеры (iOS Safari, Chrome Mobile)

#### 7. Адаптивность:

- [ ] Desktop (1920x1080)
- [ ] Laptop (1366x768)
- [ ] Tablet (768x1024)
- [ ] Mobile (375x667)

---

## 🚀 ЗАПУСК В ПРОДАКШН

### Финальный чек-лист перед запуском:

#### Безопасность:

- [ ] SSL сертификат установлен и валиден
- [ ] Все HTTP запросы редиректятся на HTTPS
- [ ] Firewall настроен (только 80, 443, 22 открыты)
- [ ] SSH доступ только по ключам (пароли отключены)
- [ ] Регулярные бэкапы настроены
- [ ] Мониторинг и алерты настроены
- [ ] Логи ротируются и архивируются

#### Производительность:

- [ ] Gzip сжатие включено
- [ ] Кеширование статических ресурсов настроено
- [ ] CDN настроен (опционально, для больших файлов)
- [ ] База данных оптимизирована (индексы, connection pooling)
- [ ] PHP/Node.js OPcache включен

#### Контент:

- [ ] Все 26 документов загружены
- [ ] Все архивы доступны для скачивания
- [ ] HTML-страницы корректно отображаются
- [ ] Кириллица везде читается правильно
- [ ] Оглавления и навигация работают
- [ ] Breadcrumbs корректны на всех страницах

#### SEO и метаданные:

- [ ] Все страницы имеют мета-теги (title, description)
- [ ] robots.txt настроен
- [ ] sitemap.xml создан и отправлен в Google/Yandex
- [ ] Open Graph теги для соцсетей

#### Юридическое соответствие:

- [ ] Политика конфиденциальности доступна и ссылка в Footer
- [ ] Пользовательское соглашение доступно и ссылка в Footer
- [ ] Cookies Banner работает и соответствует закону КР
- [ ] Обязательные чекбоксы согласия в формах регистрации

---

### Команды для финального запуска:

```bash
# 1. Последняя проверка конфигурации
sudo nginx -t
sudo systemctl reload nginx

# 2. Очистить кеш (если используется)
sudo rm -rf /var/cache/nginx/*

# 3. Проверить логи на ошибки
sudo tail -f /var/log/nginx/kfa.kg-error.log

# 4. Мониторинг доступности
watch -n 5 'curl -o /dev/null -s -w "Status: %{http_code} Time: %{time_total}s\n" https://kfa.kg'

# 5. Запустить автоматические тесты
./scripts/test-deployment.sh
```

---

## 📊 МОНИТОРИНГ И ПОДДЕРЖКА

### Настройка мониторинга:

#### 1. Uptime мониторинг:

Используйте UptimeRobot (бесплатно для 50 мониторов):

- https://kfa.kg (каждые 5 минут)
- https://kfa.kg/documents/charter/ (каждые 15 минут)
- https://kfa.kg/members/ (каждые 15 минут)

#### 2. Логирование и аналитика:

```bash
# Настройка log rotation
sudo nano /etc/logrotate.d/nginx

/var/log/nginx/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 0640 www-data adm
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
```

#### 3. Автоматические бэкапы:

```bash
# Создать скрипт /usr/local/bin/backup-kfa.sh
#!/bin/bash
DATE=$(date +%Y-%m-%d)
BACKUP_DIR="/backups/kfa"

# Бэкап файлов
tar -czf "$BACKUP_DIR/files-$DATE.tar.gz" /var/www/kfa.kg

# Бэкап базы данных
pg_dump -U postgres kfa_members > "$BACKUP_DIR/db-$DATE.sql"

# Удалить старые бэкапы (старше 30 дней)
find $BACKUP_DIR -type f -mtime +30 -delete

# Добавить в crontab:
# 0 2 * * * /usr/local/bin/backup-kfa.sh
```

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

**Технический отдел КФА:**
📧 Email: tech@kfa.kg
☎️ Телефон: +996 (312) XXX-XXX

**Для срочных вопросов:**
📱 WhatsApp/Telegram: +996 XXX XXX XXX

**Рабочие часы:**
Пн-Пт: 9:00 - 18:00 (GMT+6)

---

**Powered by:** BMAD Method v6.0
**Версия руководства:** 1.0
**Последнее обновление:** 23 октября 2025 г.

---

🚀 _"Правильное развертывание — основа стабильной работы!"_
